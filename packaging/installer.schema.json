{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Schema for install JSON file",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "Name of the application or service being packaged"
    },
    "description": {
      "type": "string",
      "description": "Summary description of the application and its installer"
    },
    "version": {
      "type": "string",
      "description": "Version string of the application installer"
    },
    "url": {
      "type": "string",
      "description": "Primary homepage or source URL for the application"
    },
    "license": {
      "oneOf": [
        {
          "type": "string",
          "description": "Single software license identifier, SPDX expression, or relative filesystem path"
        },
        {
          "type": "array",
          "description": "Structured list of component licenses bundled within this installer",
          "items": {
            "type": "object",
            "required": ["name", "license"],
            "description": "Specification of an embedded software component and its licensing terms",
            "properties": {
              "name": {
                "type": "string",
                "description": "Component or package identifier name"
              },
              "license": {
                "type": "string",
                "description": "SPDX license expression or name (e.g. GPL-2.0-only, BSD-3-Clause)"
              },
              "file": {
                "type": "string",
                "description": "Path to license text or RTF terms document"
              },
              "mandatory": {
                "type": "boolean",
                "default": true,
                "description": "Whether user acceptance of this component license is mandatory"
              }
            }
          }
        }
      ],
      "description": "Software license identifier, terms document path, or list of bundled component licenses"
    },
    "scripts_root": {
      "type": "string",
      "description": "Base directory containing custom hook and build scripts"
    },
    "wwwroot": {
      "type": "array",
      "description": "List of web service definitions, endpoints, and virtual host roots",
      "items": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Unique name identifier for this web root or virtual host"
          },
          "path": {
            "type": "string",
            "description": "Filesystem directory path containing web assets or static content"
          },
          "https": {
            "type": "object",
            "properties": {
              "provider": {
                "type": "string",
                "description": "TLS certificate provider (e.g., letsencrypt, custom, self-signed)"
              }
            },
            "required": ["provider"],
            "description": "HTTPS/TLS configuration object for this web service"
          },
          "builder": {
            "type": "array",
            "items": {
              "type": "object",
              "properties": {
                "shell": {
                  "type": "string",
                  "description": "Shell interpreter to execute the build commands (e.g., sh, bash, cmd)"
                },
                "commands": {
                  "type": "array",
                  "items": {
                    "type": "string"
                  },
                  "description": "Sequence of shell command lines executed during build"
                },
                "output": {
                  "type": "array",
                  "items": {
                    "type": "string"
                  },
                  "description": "List of expected output artifacts or directories generated by build"
                }
              },
              "required": ["commands"]
            },
            "description": "List of build steps to compile or generate static web assets"
          },
          "env": {
            "type": "string",
            "description": "Environment variable name prefix or reference for this web endpoint"
          },
          "listen": {
            "type": "string",
            "description": "Port or socket specification where the web service listens"
          },
          "dependencies": {
            "$ref": "#/definitions/Dependencies",
            "description": "Dependencies required specifically by this web service endpoint"
          },
          "domain": {
            "type": "string",
            "description": "Primary domain name for this virtual host"
          },
          "subdomain": {
            "type": "string",
            "description": "Subdomain prefix for this virtual host"
          },
          "location": {
            "type": "string",
            "description": "URL location path prefix or route for routing requests"
          }
        },
        "required": ["name", "env"]
      }
    },
    "dependencies": {
      "$ref": "#/definitions/Dependencies",
      "description": "Component and service dependencies required by the application"
    },
    "log_server": {
      "type": "object",
      "description": "Optional log server configuration for installer telemetry and tracking",
      "properties": {
        "optional": {
          "type": "boolean",
          "description": "Flag indicating whether connecting to the log server is optional"
        }
      },
      "required": []
    },
    "provides": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Virtual capabilities this installer provides (e.g., ['database', 'mysql-compatible'])"
    },
    "conflicts": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Capabilities or components this conflicts with (e.g., ['mysql-compatible'])"
    },
    "os_whitelist": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "List of supported operating systems (e.g., ['linux', 'darwin', 'windows', 'all']). Default is ['all']."
    },
    "os_blacklist": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "List of explicitly unsupported operating systems (e.g., ['windows'])."
    },
    "versions": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "List of explicit semantic versions this component can install (e.g., ['8.0.28', '8.1.17', '8.2.4']). If omitted, assumes it can satisfy any version request or 'latest'."
    },
    "ports": {
      "type": "array",
      "items": {
        "type": "integer"
      },
      "description": "List of default TCP/UDP ports this component binds to (e.g., [80, 443]). Used by the SAT solver for conflict detection."
    },
    "branding": {
      "type": "object",
      "description": "Visual branding, assets, custom licenses, and text customization for native installers (MSI, Inno, NSIS, DMG, DEB, RPM, TUI).",
      "properties": {
        "license": {
          "type": "object",
          "properties": {
            "file": {
              "type": "string",
              "description": "Path to license text or RTF file"
            },
            "type": {
              "type": "string",
              "enum": ["text", "markdown", "rtf", "html"],
              "default": "text",
              "description": "Format type of license document (text, markdown, rtf, html)"
            },
            "require_agreement": {
              "type": "boolean",
              "default": true,
              "description": "Flag requiring user to explicitly accept license before proceeding"
            },
            "agreement_checkbox_text": {
              "type": "string",
              "description": "Label text displayed next to the acceptance checkbox"
            },
            "agreement_prompt_text": {
              "type": "string",
              "description": "Prompt text displayed above the license agreement control"
            }
          },
          "description": "License display configuration for installer wizards"
        },
        "images": {
          "type": "object",
          "properties": {
            "icon": {
              "type": "string",
              "description": "Path to application icon (.ico / .icns / .png)"
            },
            "logo": {
              "type": "string",
              "description": "Primary high-res logo (.png / .bmp)"
            },
            "banner_top": {
              "type": "string",
              "description": "Top header banner image"
            },
            "banner_side": {
              "type": "string",
              "description": "Left/side welcome wizard image"
            },
            "dmg_background": {
              "type": "string",
              "description": "macOS Finder background image"
            }
          },
          "description": "Branding graphics and banner images for installer wizard dialogs"
        },
        "text": {
          "type": "object",
          "properties": {
            "welcome_title": {
              "type": "string",
              "description": "Header title displayed on the installer welcome screen"
            },
            "welcome_body": {
              "type": "string",
              "description": "Introductory body text displayed on the installer welcome screen"
            },
            "finish_title": {
              "type": "string",
              "description": "Completion title displayed on the final installer screen"
            },
            "finish_body": {
              "type": "string",
              "description": "Summary body text displayed on the final installer screen"
            }
          },
          "description": "Custom text headings and body copy displayed during installer execution"
        }
      }
    },
    "features": {
      "type": "array",
      "description": "Hierarchical feature tree definition for installer customization",
      "items": {
        "type": "object",
        "properties": {
          "id": {
            "type": "string",
            "description": "Unique identifier for this installer feature component"
          },
          "title": {
            "type": "string",
            "description": "Display title for the feature in selection trees"
          },
          "description": {
            "type": "string",
            "description": "Detailed explanation of the feature and what it installs"
          },
          "default": {
            "type": "boolean",
            "default": true,
            "description": "Whether this feature is selected by default during installation"
          },
          "dependencies": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "List of feature IDs required by this feature"
          },
          "conflicts": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "List of feature IDs conflicting with this feature"
          }
        },
        "required": ["id", "title"]
      }
    },
    "infrastructure": {
      "type": "object",
      "description": "Cloud infrastructure configuration for PaaS deployments",
      "properties": {
        "node": {
          "type": "object",
          "properties": {
            "os": {
              "type": "string",
              "description": "Operating system image (e.g., Ubuntu2204, ami-12345)"
            },
            "size": {
              "type": "string",
              "description": "Instance size or type (e.g., Standard_D4s_v3, t3.medium)"
            },
            "disk_gb": {
              "type": "integer",
              "description": "OS disk size in GB"
            }
          },
          "description": "Compute node specifications for deployment"
        },
        "network": {
          "type": "object",
          "properties": {
            "ports": {
              "type": "array",
              "items": {
                "type": "integer"
              },
              "description": "List of network ports opened in the firewall"
            },
            "cidr": {
              "type": "string",
              "description": "CIDR block specification for the virtual network subnet"
            }
          },
          "description": "Virtual network and firewall settings for deployment"
        }
      }
    }
  },
  "required": ["name", "scripts_root", "dependencies"],
  "definitions": {
    "Dependencies": {
      "type": "object",
      "properties": {
        "required": {
          "type": "object",
          "properties": {
            "databases": {
              "type": "array",
              "items": {
                "type": "object",
                "properties": {
                  "name": {
                    "type": "string",
                    "description": "Component name of the required database"
                  },
                  "version": {
                    "type": "string",
                    "description": "Version constraint for the required database"
                  },
                  "listen": {
                    "type": "string",
                    "description": "Network address and port binding for the database"
                  },
                  "env": {
                    "type": "string",
                    "description": "Environment variable name used to expose database connection"
                  },
                  "target_env": {
                    "type": "array",
                    "items": {
                      "type": "string"
                    },
                    "description": "Target environment identifiers where database is required"
                  },
                  "vars": {
                    "type": "object",
                    "description": "Custom configuration key-value pairs passed to the database"
                  },
                  "anyOf": {
                    "type": "array",
                    "items": {
                      "type": "string"
                    },
                    "description": "List of capabilities where at least one must be provided."
                  }
                },
                "required": ["version", "env"],
                "oneOf": [
                  {
                    "required": ["name"]
                  },
                  {
                    "required": ["anyOf"]
                  }
                ]
              },
              "description": "List of required database services"
            },
            "toolchains": {
              "type": "array",
              "items": {
                "type": "object",
                "properties": {
                  "name": {
                    "type": "string",
                    "description": "Component name of the required toolchain"
                  },
                  "version": {
                    "type": "string",
                    "description": "Version constraint for the required toolchain"
                  },
                  "listen": {
                    "type": "string",
                    "description": "Optional listen port binding for toolchain services"
                  },
                  "env": {
                    "type": "string",
                    "description": "Environment variable name used to expose toolchain path"
                  },
                  "anyOf": {
                    "type": "array",
                    "items": {
                      "type": "string"
                    },
                    "description": "List of capabilities where at least one must be provided."
                  }
                },
                "required": ["version", "env"],
                "oneOf": [
                  {
                    "required": ["name"]
                  },
                  {
                    "required": ["anyOf"]
                  }
                ]
              },
              "description": "List of required programming language toolchains or runtimes"
            },
            "servers": {
              "type": "array",
              "items": {
                "type": "object",
                "properties": {
                  "dest": {
                    "type": "string",
                    "description": "Installation destination directory for the server payload"
                  },
                  "listen": {
                    "type": "string",
                    "description": "Network socket address and port the server binds to"
                  },
                  "builder": {
                    "type": "array",
                    "items": {
                      "type": "object",
                      "properties": {
                        "shell": {
                          "type": "string",
                          "description": "Shell interpreter used to run build commands"
                        },
                        "commands": {
                          "type": "array",
                          "items": {
                            "type": "string"
                          },
                          "description": "Shell commands executed during application server build"
                        },
                        "output": {
                          "type": "array",
                          "items": {
                            "type": "string"
                          },
                          "description": "Output build artifacts produced by server build"
                        }
                      },
                      "required": ["commands"]
                    },
                    "description": "Build steps required to assemble the server application"
                  },
                  "daemon": {
                    "type": "object",
                    "properties": {
                      "os_native": {
                        "type": "boolean",
                        "description": "Whether the service should be managed by host init system (systemd/openrc/etc)"
                      },
                      "listen": {
                        "type": "string",
                        "description": "Socket endpoint managed by the daemon supervisor"
                      },
                      "env": {
                        "type": "array",
                        "items": {
                          "type": "string"
                        },
                        "description": "Environment variables exported to the daemon service"
                      }
                    },
                    "required": ["os_native"],
                    "description": "Daemonization and system service configuration"
                  },
                  "name": {
                    "type": "string",
                    "description": "Component name of the application server"
                  },
                  "anyOf": {
                    "type": "array",
                    "items": {
                      "type": "string"
                    },
                    "description": "List of capabilities where at least one must be provided."
                  }
                },
                "required": ["daemon"],
                "oneOf": [
                  {
                    "required": ["name"]
                  },
                  {
                    "required": ["anyOf"]
                  }
                ]
              },
              "description": "List of required application servers or background services"
            },
            "capabilities": {
              "type": "array",
              "items": {
                "type": "string"
              },
              "description": "Virtual capabilities required by this installer (e.g., ['web-server'])"
            }
          },
          "required": [],
          "description": "Mandatory dependencies that must be satisfied for installation"
        },
        "optional": {
          "type": "object",
          "properties": {
            "databases": {
              "type": "array",
              "items": {
                "type": "object",
                "properties": {
                  "name": {
                    "type": "string",
                    "description": "Component name of the optional database"
                  },
                  "version": {
                    "type": "string",
                    "description": "Version constraint for the optional database"
                  },
                  "listen": {
                    "type": "string",
                    "description": "Network address and port binding for optional database"
                  },
                  "env": {
                    "type": "string",
                    "description": "Environment variable name for optional database connection"
                  },
                  "vars": {
                    "type": "object",
                    "description": "Custom configuration key-value pairs for optional database"
                  },
                  "anyOf": {
                    "type": "array",
                    "items": {
                      "type": "string"
                    },
                    "description": "List of capabilities where at least one must be provided."
                  }
                },
                "required": ["version", "env"],
                "oneOf": [
                  {
                    "required": ["name"]
                  },
                  {
                    "required": ["anyOf"]
                  }
                ]
              },
              "description": "List of optional database services"
            },
            "capabilities": {
              "type": "array",
              "items": {
                "type": "string"
              },
              "description": "Virtual capabilities required by this installer (e.g., ['web-server'])"
            }
          },
          "required": [],
          "description": "Optional dependencies that enhance application capabilities"
        }
      },
      "required": []
    }
  }
}
