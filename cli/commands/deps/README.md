# LibScript Dependency Commands

## Overview

Resolves and automates dependency installation pipelines defined in component schemas or
`libscript.json` files.

## Subcommands

- `install`: Resolves dependencies with customizable reuse or isolation strategies.
- `process_downloads`: Batch-processes remote artifact downloads using `aria2` or `curl`.
