# LibScript Packaging Tools

## Overview

Provides tools to containerize and package LibScript components into distributable artifacts.

## Usage

```sh
./libscript.sh package-as <format> [options]
```

Supported formats are detailed in the `formats/` directory.
