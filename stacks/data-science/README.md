# Data Science Stacks

This directory contains pre-configured application stack definitions and orchestration workflows for
data science solutions in LibScript.

## Available Stacks

- **[jupyterhub](jupyterhub/)**: This document describes the `JupyterHub` component located in the
  `third_party` folder within the

## Usage

To provision and run any stack in this category:

```sh
./libscript.sh run <stack-name> latest
```
