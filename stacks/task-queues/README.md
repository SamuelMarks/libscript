# Task Queues Stacks

This directory contains pre-configured application stack definitions and orchestration workflows for
task queues solutions in LibScript.

## Available Stacks

- **[celery](celery/)**: This document describes the `celery` task queue and worker component (part
  of `_storage`) within the

## Usage

To provision and run any stack in this category:

```sh
./libscript.sh run <stack-name> latest
```
