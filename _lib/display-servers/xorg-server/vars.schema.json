{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "/_lib/display-servers/xorg-server/vars.schema.json",
  "title": "Xorg-Server variables",
  "description": "Configuration variables for Xorg-Server",
  "type": "object",
  "properties": {
    "XORG_SERVER_VERSION": {
      "description": "Specific version of xorg-server to install.",
      "type": "string",
      "default": "latest"
    },
    "XORG_SERVER_INSTALL_METHOD": {
      "type": "string",
      "description": "How to install XORG_SERVER. 'libscript_native' uses isolated version dirs, 'system' uses OS package manager.",
      "default": "system"
    }
  }
}
