{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "/_lib/toolchains/kubectl/vars.schema.json",
  "title": "Kubectl variables",
  "description": "Configuration variables for Kubectl",
  "type": "object",
  "properties": {
    "KUBECTL_VERSION": {
      "description": "Specific version of kubectl to install. Can be a numeric version or an alias.",
      "type": "string",
      "default": "latest",
      "version_aliases": ["latest", "stable"]
    },
    "KUBECTL_INSTALL_METHOD": {
      "description": "How to install KUBECTL. 'libscript_native' uses isolated version dirs, 'system' uses OS package manager, 'mise' or 'asdf' defers to third-party tools.",
      "type": "string",
      "enum": ["libscript_native", "system", "mise", "asdf"],
      "default": "libscript_native"
    },
    "LIBSCRIPT_LISTEN_PORT": {
      "type": ["string", "integer"],
      "description": "Global port to listen on"
    },
    "LIBSCRIPT_LISTEN_ADDRESS": {
      "type": "string",
      "description": "Global address to listen on"
    },
    "LIBSCRIPT_LISTEN_SOCKET": {
      "type": "string",
      "description": "Global unix socket to listen on"
    },
    "KUBECTL_LISTEN_PORT": {
      "type": ["string", "integer"],
      "description": "Port for KUBECTL to listen on"
    },
    "KUBECTL_LISTEN_ADDRESS": {
      "type": "string",
      "description": "Address for KUBECTL to listen on"
    },
    "KUBECTL_LISTEN_SOCKET": {
      "type": "string",
      "description": "Unix socket for KUBECTL to listen on"
    }
  }
}
