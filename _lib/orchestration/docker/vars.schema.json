{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "/_lib/orchestration/docker/vars.schema.json",
  "title": "docker vars",
  "description": "Variables for Docker",
  "type": "object",
  "properties": {
    "DOCKER_VERSION": {
      "description": "Specific version of docker to install. Can be a numeric version or an alias.",
      "type": "string",
      "default": "latest",
      "version_aliases": ["latest", "stable"]
    },
    "DOCKER_INSTALL_METHOD": {
      "description": "How to install DOCKER. 'libscript_native' uses isolated version dirs, 'system' uses OS package manager, 'mise', 'asdf', 'pkgx', or 'vfox' defers to third-party tools.",
      "type": "string",
      "enum": ["libscript_native", "system", "mise", "asdf", "pkgx", "vfox"],
      "default": "libscript_native"
    },
    "LIBSCRIPT_LISTEN_PORT": {
      "type": ["string", "integer"],
      "description": "Global port to listen on"
    },
    "LIBSCRIPT_LISTEN_ADDRESS": {
      "type": "string",
      "description": "Global address to listen on"
    },
    "LIBSCRIPT_LISTEN_SOCKET": {
      "type": "string",
      "description": "Global unix socket to listen on"
    },
    "DOCKER_LISTEN_PORT": {
      "type": ["string", "integer"],
      "description": "Port for DOCKER to listen on"
    },
    "DOCKER_LISTEN_ADDRESS": {
      "type": "string",
      "description": "Address for DOCKER to listen on"
    },
    "DOCKER_LISTEN_SOCKET": {
      "type": "string",
      "description": "Unix socket for DOCKER to listen on"
    }
  }
}
