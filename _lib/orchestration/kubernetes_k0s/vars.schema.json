{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "/_lib/orchestration/kubernetes_k0s/vars.schema.json",
  "title": "kubernetes_k0s vars",
  "description": "Variables for Kubernetes k0s",
  "type": "object",
  "properties": {
    "KUBERNETES_K0S_VERSION": {
      "description": "Specific version of k0s to install",
      "type": "string",
      "default": "latest"
    },
    "DEST": {
      "description": "Destination (working directory)",
      "type": "string"
    },
    "VARS": {
      "description": "Key/value in JSON format (as an escaped string)",
      "type": "string"
    },
    "LIBSCRIPT_LISTEN_PORT": {
      "type": [
        "string",
        "integer"
      ],
      "description": "Global port to listen on"
    },
    "LIBSCRIPT_LISTEN_ADDRESS": {
      "type": "string",
      "description": "Global address to listen on"
    },
    "LIBSCRIPT_LISTEN_SOCKET": {
      "type": "string",
      "description": "Global unix socket to listen on"
    },
    "KUBERNETES_K0S_LISTEN_PORT": {
      "type": [
        "string",
        "integer"
      ],
      "description": "Port for KUBERNETES_K0S to listen on"
    },
    "KUBERNETES_K0S_LISTEN_ADDRESS": {
      "type": "string",
      "description": "Address for KUBERNETES_K0S to listen on"
    },
    "KUBERNETES_K0S_LISTEN_SOCKET": {
      "type": "string",
      "description": "Unix socket for KUBERNETES_K0S to listen on"
    }
  }
}
