{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "/_lib/orchestration/runner/vars.schema.json",
  "title": "Runner variables",
  "description": "Configuration variables for Runner",
  "type": "object",
  "properties": {
    "RUNNER_VERSION": {
      "description": "Specific version of runner to install.",
      "type": "string",
      "default": "latest"
    },
    "RUNNER_INSTALL_METHOD": {
      "type": "string",
      "description": "How to install RUNNER. 'libscript_native' uses isolated version dirs, 'system' uses OS package manager.",
      "default": "system"
    }
  }
}
