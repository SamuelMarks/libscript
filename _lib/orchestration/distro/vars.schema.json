{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "/_lib/orchestration/distro/vars.schema.json",
  "title": "Distro variables",
  "description": "Configuration variables for Distro",
  "type": "object",
  "properties": {
    "DISTRO_VERSION": {
      "description": "Specific version of distro to install.",
      "type": "string",
      "default": "latest"
    },
    "DISTRO_INSTALL_METHOD": {
      "type": "string",
      "description": "How to install DISTRO. 'libscript_native' uses isolated version dirs, 'system' uses OS package manager.",
      "default": "system"
    }
  }
}
