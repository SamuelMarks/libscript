{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "/_lib/storage-layers/minio/vars.schema.json",
  "title": "MinIO variables",
  "description": "Configuration variables for MinIO",
  "type": "object",
  "properties": {
    "MINIO_INSTALL_METHOD": {
      "description": "How to install MINIO. 'libscript_native' uses isolated version dirs, 'system' uses OS package manager, 'mise', 'asdf', 'pkgx', or 'vfox' defers to third-party tools.",
      "type": "string",
      "default": "libscript_native"
    },
    "MINIO_VERSION": {
      "description": "Specific version of minio to install. Can be a numeric version or an alias.",
      "type": "string",
      "default": "latest"
    }
  }
}
