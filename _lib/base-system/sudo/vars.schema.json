{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "/_lib/base-system/sudo/vars.schema.json",
  "title": "Sudo variables",
  "description": "Configuration variables for Sudo",
  "type": "object",
  "properties": {
    "SUDO_VERSION": {
      "description": "Specific version of sudo to install.",
      "type": "string",
      "default": "latest"
    },
    "SUDO_INSTALL_METHOD": {
      "type": "string",
      "description": "How to install SUDO. 'libscript_native' uses isolated version dirs, 'system' uses OS package manager.",
      "default": "system"
    }
  }
}
