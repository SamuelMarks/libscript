{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "/_lib/base-system/xfsprogs/vars.schema.json",
  "title": "Xfsprogs variables",
  "description": "Configuration variables for Xfsprogs",
  "type": "object",
  "properties": {
    "XFSPROGS_VERSION": {
      "description": "Specific version of xfsprogs to install.",
      "type": "string",
      "default": "latest"
    },
    "XFSPROGS_INSTALL_METHOD": {
      "type": "string",
      "description": "How to install XFSPROGS. 'libscript_native' uses isolated version dirs, 'system' uses OS package manager.",
      "default": "system"
    }
  }
}
