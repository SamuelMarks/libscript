{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "/_lib/base-system/pam/vars.schema.json",
  "title": "PAM variables",
  "description": "Configuration variables for PAM",
  "type": "object",
  "properties": {
    "PAM_VERSION": {
      "description": "Specific version of pam to install.",
      "type": "string",
      "default": "latest"
    },
    "PAM_INSTALL_METHOD": {
      "type": "string",
      "description": "How to install PAM. 'libscript_native' uses isolated version dirs, 'system' uses OS package manager.",
      "default": "system"
    }
  }
}
