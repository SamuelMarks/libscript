# Musl C Library

Lightweight, fast, simple, standards-conformant C standard library.

## Purpose & Current State

This document provides context and technical details for the **Musl C Library** component (part of
the `base-system` module) within the LibScript ecosystem. Musl is an implementation of the C
standard library designed for lightweight, statically-linkable, and secure POSIX environments.

## Usage

You can install this component using the global `libscript` command.

**Unix (Linux/macOS):**

```sh
./libscript.sh install musl
```

**Windows:**

```cmd
libscript.cmd install musl
```

## Configuration Options

The following environment variables can be passed to the CLI (`--KEY=VALUE`) or exported before
running the setup script.

<!-- BEGIN_VARS -->
<!-- END_VARS -->

## Platform Support

<!-- BEGIN_PLATFORMS -->
<!-- END_PLATFORMS -->
