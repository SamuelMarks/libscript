{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "/_lib/_storage/postgres/vars.schema.json",
  "title": "postgres server vars",
  "description": "postgres vars that can be set",
  "type": "object",
  "properties": {
    "POSTGRESQL_VERSION": {
      "description": "Version of PostgreSQL demanded",
      "type": "string"
    },
    "POSTGRES_USER": {
      "description": "Username to create inside postgres",
      "type": "string"
    },
    "POSTGRES_PASSWORD": {
      "description": "Password for created user inside postgres",
      "type": "string"
    },
    "POSTGRES_PASSWORD_FILE": {
      "description": "Password file, its contents used as password for created user inside postgres",
      "type": "string"
    },
    "POSTGRES_SERVICE_USER": {
      "description": "Username for superuser & system role",
      "type": "string"
    },
    "POSTGRES_SERVICE_PASSWORD": {
      "description": "Password for postgres user; fallsback to `POSTGRES_PASSWORD`",
      "type": "string"
    },
    "POSTGRES_HOST": {
      "description": "Hostname to serve postgres from",
      "type": "string"
    },
    "POSTGRES_DB": {
      "description": "Database to create within postgres",
      "type": "string"
    },
    "LIBSCRIPT_GLOBAL_INSTALL_METHOD": {
      "description": "Global override for how software should be installed across all systems (e.g. system package manager vs downloaded binaries/from-source).",
      "type": "string",
      "enum": [
        "system",
        "source"
      ],
      "default": "system"
    },
    "LIBSCRIPT_WINDOWS_PKG_MGR": {
      "description": "Global package manager override for Windows targets (e.g. winget, choco).",
      "type": "string",
      "enum": [
        "winget",
        "choco"
      ],
      "default": "winget"
    }
  }
}