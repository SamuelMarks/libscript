{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "/_lib/_server/nginx/vars.schema.json",
  "title": "nginx vars",
  "description": "Nginx configuration variables",
  "type": "object",
  "properties": {
    "NGINX_INSTALL_METHOD": {
      "description": "How to install Nginx.",
      "type": "string",
      "enum": [
        "system",
        "source"
      ],
      "default": "system"
    },
    "WWWROOT_NAME": {
      "description": "The server name/domain (e.g. example.com)",
      "type": "string"
    },
    "WWWROOT_PATH": {
      "description": "The path to the document root",
      "type": "string"
    },
    "WWWROOT_LISTEN": {
      "description": "The port Nginx should listen on",
      "type": "string",
      "default": "80"
    },
    "LIBSCRIPT_GLOBAL_INSTALL_METHOD": {
      "description": "Global override for how software should be installed across all systems (e.g. system package manager vs downloaded binaries/from-source).",
      "type": "string",
      "enum": [
        "system",
        "source"
      ],
      "default": "system"
    },
    "LIBSCRIPT_WINDOWS_PKG_MGR": {
      "description": "Global package manager override for Windows targets (e.g. winget, choco).",
      "type": "string",
      "enum": [
        "winget",
        "choco"
      ],
      "default": "winget"
    }
  }
}