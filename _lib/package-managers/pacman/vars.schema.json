{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "/_lib/package-managers/pacman/vars.schema.json",
  "title": "Pacman variables",
  "description": "Configuration variables for pacman",
  "type": "object",
  "properties": {
    "PACMAN_INSTALL_METHOD": {
      "description": "How to install PACMAN. 'libscript_native' uses isolated version dirs, 'system' uses OS package manager, 'mise', 'asdf', 'pkgx', or 'vfox' defers to third-party tools.",
      "type": "string",
      "default": "libscript_native"
    },
    "PACMAN_VERSION": {
      "description": "Specific version of pacman to install.",
      "type": "string",
      "default": "latest"
    }
  }
}
