{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "/_lib/package-managers/pub/vars.schema.json",
  "title": "Pub variables",
  "description": "Configuration variables for pub",
  "type": "object",
  "properties": {
    "PUB_INSTALL_METHOD": {
      "description": "How to install PUB. 'libscript_native' uses isolated version dirs, 'system' uses OS package manager, 'mise', 'asdf', 'pkgx', or 'vfox' defers to third-party tools.",
      "type": "string",
      "default": "libscript_native"
    },
    "PUB_VERSION": {
      "description": "Specific version of pub to install.",
      "type": "string",
      "default": "latest"
    }
  }
}
