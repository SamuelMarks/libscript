{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "/_lib/package-managers/rvm/vars.schema.json",
  "title": "Rvm variables",
  "description": "Configuration variables for rvm",
  "type": "object",
  "properties": {
    "RVM_INSTALL_METHOD": {
      "description": "How to install RVM. 'libscript_native' uses isolated version dirs, 'system' uses OS package manager, 'mise', 'asdf', 'pkgx', or 'vfox' defers to third-party tools.",
      "type": "string",
      "default": "libscript_native"
    },
    "RVM_VERSION": {
      "description": "Specific version of rvm to install.",
      "type": "string",
      "default": "latest"
    }
  }
}
