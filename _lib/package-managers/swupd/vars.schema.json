{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "/_lib/package-managers/swupd/vars.schema.json",
  "title": "Swupd variables",
  "description": "Configuration variables for swupd",
  "type": "object",
  "properties": {
    "SWUPD_INSTALL_METHOD": {
      "description": "How to install SWUPD. 'libscript_native' uses isolated version dirs, 'system' uses OS package manager, 'mise', 'asdf', 'pkgx', or 'vfox' defers to third-party tools.",
      "type": "string",
      "default": "libscript_native"
    },
    "SWUPD_VERSION": {
      "description": "Specific version of swupd to install.",
      "type": "string",
      "default": "latest"
    }
  }
}
