{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "/_lib/package-managers/mas/vars.schema.json",
  "title": "Mas variables",
  "description": "Configuration variables for mas",
  "type": "object",
  "properties": {
    "MAS_INSTALL_METHOD": {
      "description": "How to install MAS. 'libscript_native' uses isolated version dirs, 'system' uses OS package manager, 'mise', 'asdf', 'pkgx', or 'vfox' defers to third-party tools.",
      "type": "string",
      "default": "libscript_native"
    },
    "MAS_VERSION": {
      "description": "Specific version of mas to install.",
      "type": "string",
      "default": "latest"
    }
  }
}
