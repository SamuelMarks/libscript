{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "/_lib/package-managers/dnf/vars.schema.json",
  "title": "Dnf variables",
  "description": "Configuration variables for dnf",
  "type": "object",
  "properties": {
    "DNF_INSTALL_METHOD": {
      "description": "How to install DNF. 'libscript_native' uses isolated version dirs, 'system' uses OS package manager, 'mise', 'asdf', 'pkgx', or 'vfox' defers to third-party tools.",
      "type": "string",
      "default": "libscript_native"
    },
    "DNF_VERSION": {
      "description": "Specific version of dnf to install.",
      "type": "string",
      "default": "latest"
    }
  }
}
