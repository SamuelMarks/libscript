{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "/_lib/package-managers/nimble/vars.schema.json",
  "title": "Nimble variables",
  "description": "Configuration variables for nimble",
  "type": "object",
  "properties": {
    "NIMBLE_INSTALL_METHOD": {
      "description": "How to install NIMBLE. 'libscript_native' uses isolated version dirs, 'system' uses OS package manager, 'mise', 'asdf', 'pkgx', or 'vfox' defers to third-party tools.",
      "type": "string",
      "default": "libscript_native"
    },
    "NIMBLE_CHOOSENIM_DIR": {
      "type": "string",
      "description": "Installation directory for choosenim. Defaults to ~/.choosenim."
    },
    "NIMBLE_CHOOSENIM_CHOOSE_VERSION": {
      "type": "string",
      "description": "Specific Nim version to install initially."
    },
    "NIMBLE_VERSION": {
      "description": "Specific version of nimble to install.",
      "type": "string",
      "default": "latest"
    }
  }
}
