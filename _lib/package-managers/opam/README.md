Opam
====

Bootstrap script for [opam](https://opam.ocaml.org/), the source-based package manager for OCaml.

## Platform Support
- macOS
- Linux
- Windows (Not Supported natively, mostly requires WSL)

## Variables
See `vars.schema.json` for details on available variables.
