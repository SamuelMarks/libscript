{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "/_lib/package-managers/opam/vars.schema.json",
  "title": "Opam variables",
  "description": "Configuration variables for opam",
  "type": "object",
  "properties": {
    "OPAM_INSTALL_METHOD": {
      "description": "How to install OPAM. 'libscript_native' uses isolated version dirs, 'system' uses OS package manager, 'mise', 'asdf', 'pkgx', or 'vfox' defers to third-party tools.",
      "type": "string",
      "default": "libscript_native"
    },
    "OPAM_VERSION": {
      "description": "Specific version of opam to install.",
      "type": "string",
      "default": "latest"
    }
  }
}
