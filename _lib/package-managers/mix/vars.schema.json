{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "/_lib/package-managers/mix/vars.schema.json",
  "title": "Mix variables",
  "description": "Configuration variables for mix",
  "type": "object",
  "properties": {
    "MIX_INSTALL_METHOD": {
      "description": "How to install MIX. 'libscript_native' uses isolated version dirs, 'system' uses OS package manager, 'mise', 'asdf', 'pkgx', or 'vfox' defers to third-party tools.",
      "type": "string",
      "default": "libscript_native"
    },
    "MIX_VERSION": {
      "description": "Specific version of mix to install.",
      "type": "string",
      "default": "latest"
    }
  }
}
