{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "/_lib/package-managers/macports/vars.schema.json",
  "title": "Macports variables",
  "description": "Configuration variables for macports",
  "type": "object",
  "properties": {
    "MACPORTS_INSTALL_METHOD": {
      "description": "How to install MACPORTS. 'libscript_native' uses isolated version dirs, 'system' uses OS package manager, 'mise', 'asdf', 'pkgx', or 'vfox' defers to third-party tools.",
      "type": "string",
      "default": "libscript_native"
    },
    "MACPORTS_VERSION": {
      "description": "Specific version of macports to install.",
      "type": "string",
      "default": "latest"
    }
  }
}
