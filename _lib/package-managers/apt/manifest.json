{
  "$schema": "../../_common/manifest.schema.json",
  "name": "apt",
  "provides": ["apt"],
  "os_whitelist": ["debian", "ubuntu"],
  "title": "apt",
  "description": "Bootstrap module for the `apt` package manager.",
  "category": "package-manager"
}
