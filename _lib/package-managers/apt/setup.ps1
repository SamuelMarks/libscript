<#
.SYNOPSIS
Orchestrates the setup and installation process for the component 'apt' stack.

.DESCRIPTION
Execute this script to install and configure apt on the local system.
#>

$ErrorActionPreference = "Stop"

Write-Host "Bootstrapping apt is not supported natively on Windows."
