{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "/_lib/package-managers/krew/vars.schema.json",
  "title": "Krew variables",
  "description": "Configuration variables for krew",
  "type": "object",
  "properties": {
    "KREW_INSTALL_METHOD": {
      "description": "How to install KREW. 'libscript_native' uses isolated version dirs, 'system' uses OS package manager, 'mise', 'asdf', 'pkgx', or 'vfox' defers to third-party tools.",
      "type": "string",
      "default": "libscript_native"
    },
    "KREW_ROOT": {
      "type": "string",
      "description": "Installation directory for krew and plugins. Defaults to ~/.krew."
    },
    "KREW_VERSION": {
      "description": "Specific version of krew to install.",
      "type": "string",
      "default": "latest"
    }
  }
}
