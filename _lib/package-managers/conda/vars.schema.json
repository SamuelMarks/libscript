{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "/_lib/package-managers/conda/vars.schema.json",
  "title": "Conda variables",
  "description": "Configuration variables for conda",
  "type": "object",
  "properties": {
    "CONDA_INSTALL_METHOD": {
      "description": "How to install CONDA. 'libscript_native' uses isolated version dirs, 'system' uses OS package manager, 'mise', 'asdf', 'pkgx', or 'vfox' defers to third-party tools.",
      "type": "string",
      "default": "libscript_native"
    },
    "CONDA_VERSION": {
      "description": "Specific version of conda to install.",
      "type": "string",
      "default": "latest"
    }
  }
}
