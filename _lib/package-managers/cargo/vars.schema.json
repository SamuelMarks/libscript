{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "/_lib/package-managers/cargo/vars.schema.json",
  "title": "Cargo variables",
  "description": "Configuration variables for cargo",
  "type": "object",
  "properties": {
    "CARGO_INSTALL_METHOD": {
      "description": "How to install CARGO. 'libscript_native' uses isolated version dirs, 'system' uses OS package manager, 'mise', 'asdf', 'pkgx', or 'vfox' defers to third-party tools.",
      "type": "string",
      "default": "libscript_native"
    },
    "CARGO_VERSION": {
      "description": "Specific version of cargo to install.",
      "type": "string",
      "default": "latest"
    }
  }
}
