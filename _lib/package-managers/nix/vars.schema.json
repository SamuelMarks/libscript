{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "/_lib/package-managers/nix/vars.schema.json",
  "title": "Nix variables",
  "type": "object",
  "properties": {
    "NIX_INSTALL_METHOD": {
      "description": "How to install NIX. 'libscript_native' uses isolated version dirs, 'system' uses OS package manager, 'mise', 'asdf', 'pkgx', or 'vfox' defers to third-party tools.",
      "type": "string",
      "default": "libscript_native"
    },
    "NIX_VERSION": {
      "description": "Specific version of nix to install.",
      "type": "string",
      "default": "latest"
    }
  },
  "description": "Configuration variables for nix"
}
