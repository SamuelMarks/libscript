{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "/_lib/package-managers/sbt/vars.schema.json",
  "title": "Sbt variables",
  "description": "Configuration variables for sbt",
  "type": "object",
  "properties": {
    "SBT_VERSION": {
      "description": "Specific version of sbt to install.",
      "type": "string",
      "default": "latest"
    },
    "SBT_INSTALL_METHOD": {
      "description": "How to install SBT. 'libscript_native' uses isolated version dirs, 'system' uses OS package manager, 'mise', 'asdf', 'pkgx', or 'vfox' defers to third-party tools.",
      "type": "string",
      "enum": ["libscript_native", "system", "mise", "asdf", "pkgx", "vfox"],
      "default": "libscript_native"
    }
  }
}
