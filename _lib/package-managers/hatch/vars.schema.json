{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "/_lib/package-managers/hatch/vars.schema.json",
  "title": "Hatch variables",
  "description": "Configuration variables for hatch",
  "type": "object",
  "properties": {
    "HATCH_INSTALL_METHOD": {
      "description": "How to install HATCH. 'libscript_native' uses isolated version dirs, 'system' uses OS package manager, 'mise', 'asdf', 'pkgx', or 'vfox' defers to third-party tools.",
      "type": "string",
      "default": "libscript_native"
    },
    "HATCH_VERSION": {
      "description": "Specific version of hatch to install.",
      "type": "string",
      "default": "latest"
    }
  }
}
