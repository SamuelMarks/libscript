{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "/_lib/package-managers/flatpak/vars.schema.json",
  "title": "Flatpak variables",
  "description": "Configuration variables for flatpak",
  "type": "object",
  "properties": {
    "FLATPAK_INSTALL_METHOD": {
      "description": "How to install FLATPAK. 'libscript_native' uses isolated version dirs, 'system' uses OS package manager, 'mise', 'asdf', 'pkgx', or 'vfox' defers to third-party tools.",
      "type": "string",
      "default": "libscript_native"
    },
    "FLATPAK_VERSION": {
      "description": "Specific version of flatpak to install.",
      "type": "string",
      "default": "latest"
    }
  }
}
