Rbenv
=====

Bootstrap module for the `rbenv` package manager.

## Usage
Ensures the `rbenv` executable is available. This relies on the core language toolchain appropriate for the tool.

## Platform Support
- **Linux**: Supported.
- **macOS**: Supported.
- **Windows**: Supported.

## Variables
See `vars.schema.json` for details on available variables.
