{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "/_lib/package-managers/pkg/vars.schema.json",
  "title": "Pkg variables",
  "description": "Configuration variables for pkg",
  "type": "object",
  "properties": {
    "PKG_INSTALL_METHOD": {
      "description": "How to install PKG. 'libscript_native' uses isolated version dirs, 'system' uses OS package manager, 'mise', 'asdf', 'pkgx', or 'vfox' defers to third-party tools.",
      "type": "string",
      "default": "libscript_native"
    },
    "PKG_VERSION": {
      "description": "Specific version of pkg to install.",
      "type": "string",
      "default": "latest"
    }
  }
}
