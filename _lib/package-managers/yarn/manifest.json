{
  "$schema": "../../_common/manifest.schema.json",
  "name": "yarn",
  "provides": ["package-manager", "yarn"],
  "os_whitelist": ["all"],
  "os_blacklist": ["sunos"],
  "title": "yarn",
  "description": "Bootstrap module for the `yarn` package manager.",
  "category": "package-manager"
}
