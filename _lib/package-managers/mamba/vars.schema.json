{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "/_lib/package-managers/mamba/vars.schema.json",
  "title": "Mamba variables",
  "description": "Configuration variables for mamba",
  "type": "object",
  "properties": {
    "MAMBA_INSTALL_METHOD": {
      "description": "How to install MAMBA. 'libscript_native' uses isolated version dirs, 'system' uses OS package manager, 'mise', 'asdf', 'pkgx', or 'vfox' defers to third-party tools.",
      "type": "string",
      "default": "libscript_native"
    },
    "MAMBA_VERSION": {
      "description": "Specific version of mamba to install.",
      "type": "string",
      "default": "latest"
    }
  }
}
