{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "/_lib/package-managers/apk/vars.schema.json",
  "title": "Apk variables",
  "description": "Configuration variables for apk",
  "type": "object",
  "properties": {
    "APK_VERSION": {
      "description": "Specific version of apk to install.",
      "type": "string",
      "default": "latest"
    },
    "APK_INSTALL_METHOD": {
      "description": "How to install APK. 'libscript_native' uses isolated version dirs, 'system' uses OS package manager, 'mise', 'asdf', 'pkgx', or 'vfox' defers to third-party tools.",
      "type": "string",
      "enum": ["libscript_native", "system", "mise", "asdf", "pkgx", "vfox"],
      "default": "libscript_native"
    }
  }
}
