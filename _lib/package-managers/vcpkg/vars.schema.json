{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "/_lib/package-managers/vcpkg/vars.schema.json",
  "title": "Vcpkg variables",
  "description": "Configuration variables for vcpkg",
  "type": "object",
  "properties": {
    "VCPKG_INSTALL_METHOD": {
      "description": "How to install VCPKG. 'libscript_native' uses isolated version dirs, 'system' uses OS package manager, 'mise', 'asdf', 'pkgx', or 'vfox' defers to third-party tools.",
      "type": "string",
      "default": "libscript_native"
    },
    "VCPKG_VERSION": {
      "description": "Specific version of vcpkg to install.",
      "type": "string",
      "default": "latest"
    }
  }
}
