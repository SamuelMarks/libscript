{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "/_lib/package-managers/awscli/vars.schema.json",
  "title": "Awscli variables",
  "description": "Configuration variables for awscli",
  "type": "object",
  "properties": {
    "AWSCLI_INSTALL_METHOD": {
      "description": "How to install AWSCLI. 'libscript_native' uses isolated version dirs, 'system' uses OS package manager, 'mise', 'asdf', 'pkgx', or 'vfox' defers to third-party tools.",
      "type": "string",
      "default": "libscript_native"
    },
    "AWSCLI_VERSION": {
      "type": "string",
      "default": "latest",
      "description": "Version of AWS CLI to install"
    }
  }
}
