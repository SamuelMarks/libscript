# awscli Package Manager Config

Provides bootstrap definitions and wrappers for installing the AWS CLI natively via OS package managers or official installers.

## Configuration
See `vars.schema.json` for installation options and version pinning.
