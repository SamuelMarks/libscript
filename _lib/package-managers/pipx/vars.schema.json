{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "/_lib/package-managers/pipx/vars.schema.json",
  "title": "Pipx variables",
  "description": "Configuration variables for pipx",
  "type": "object",
  "properties": {
    "PIPX_INSTALL_METHOD": {
      "description": "How to install PIPX. 'libscript_native' uses isolated version dirs, 'system' uses OS package manager, 'mise', 'asdf', 'pkgx', or 'vfox' defers to third-party tools.",
      "type": "string",
      "default": "libscript_native"
    },
    "PIPX_VERSION": {
      "description": "Specific version of pipx to install.",
      "type": "string",
      "default": "latest"
    }
  }
}
