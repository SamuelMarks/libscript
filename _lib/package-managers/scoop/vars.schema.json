{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "/_lib/package-managers/scoop/vars.schema.json",
  "title": "Scoop variables",
  "description": "Configuration variables for scoop",
  "type": "object",
  "properties": {
    "SCOOP_INSTALL_METHOD": {
      "description": "How to install SCOOP. 'libscript_native' uses isolated version dirs, 'system' uses OS package manager, 'mise', 'asdf', 'pkgx', or 'vfox' defers to third-party tools.",
      "type": "string",
      "default": "libscript_native"
    },
    "SCOOP_VERSION": {
      "description": "Specific version of scoop to install.",
      "type": "string",
      "default": "latest"
    }
  }
}
