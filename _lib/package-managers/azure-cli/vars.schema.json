{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "/_lib/package-managers/azure-cli/vars.schema.json",
  "title": "Azure Cli variables",
  "description": "Configuration variables for azure-cli",
  "type": "object",
  "properties": {
    "AZURE_CLI_INSTALL_METHOD": {
      "description": "How to install AZURE_CLI. 'libscript_native' uses isolated version dirs, 'system' uses OS package manager, 'mise', 'asdf', 'pkgx', or 'vfox' defers to third-party tools.",
      "type": "string",
      "default": "libscript_native"
    },
    "AZURE_CLI_VERSION": {
      "type": "string",
      "default": "latest",
      "description": "Version of Azure CLI to install"
    }
  }
}
