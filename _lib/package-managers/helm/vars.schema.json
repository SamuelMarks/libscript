{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "/_lib/package-managers/helm/vars.schema.json",
  "title": "Helm variables",
  "description": "Configuration variables for helm",
  "type": "object",
  "properties": {
    "HELM_INSTALL_METHOD": {
      "description": "How to install HELM. 'libscript_native' uses isolated version dirs, 'system' uses OS package manager, 'mise', 'asdf', 'pkgx', or 'vfox' defers to third-party tools.",
      "type": "string",
      "default": "libscript_native"
    },
    "HELM_VERSION": {
      "description": "Specific version of helm to install.",
      "type": "string",
      "default": "latest"
    }
  }
}
