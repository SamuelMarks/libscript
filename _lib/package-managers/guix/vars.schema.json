{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "/_lib/package-managers/guix/vars.schema.json",
  "title": "Guix variables",
  "description": "Configuration variables for guix",
  "type": "object",
  "properties": {
    "GUIX_INSTALL_METHOD": {
      "description": "How to install GUIX. 'libscript_native' uses isolated version dirs, 'system' uses OS package manager, 'mise', 'asdf', 'pkgx', or 'vfox' defers to third-party tools.",
      "type": "string",
      "default": "libscript_native"
    },
    "GUIX_VERSION": {
      "description": "Specific version of guix to install.",
      "type": "string",
      "default": "latest"
    }
  }
}
