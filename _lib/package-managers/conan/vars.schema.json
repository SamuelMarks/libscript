{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "/_lib/package-managers/conan/vars.schema.json",
  "title": "Conan variables",
  "description": "Configuration variables for conan",
  "type": "object",
  "properties": {
    "CONAN_INSTALL_METHOD": {
      "description": "How to install CONAN. 'libscript_native' uses isolated version dirs, 'system' uses OS package manager, 'mise', 'asdf', 'pkgx', or 'vfox' defers to third-party tools.",
      "type": "string",
      "default": "libscript_native"
    },
    "CONAN_VERSION": {
      "description": "Specific version of conan to install.",
      "type": "string",
      "default": "latest"
    }
  }
}
