{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "/_lib/package-managers/uv/vars.schema.json",
  "title": "Uv variables",
  "description": "Configuration variables for uv",
  "type": "object",
  "properties": {
    "UV_INSTALL_METHOD": {
      "description": "How to install UV. 'libscript_native' uses isolated version dirs, 'system' uses OS package manager, 'mise', 'asdf', 'pkgx', or 'vfox' defers to third-party tools.",
      "type": "string",
      "default": "system"
    },
    "UV_VERSION": {
      "description": "Specific version of uv to install.",
      "type": "string",
      "default": "latest"
    }
  }
}
