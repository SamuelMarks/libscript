{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "/_lib/package-managers/choco/vars.schema.json",
  "title": "Choco variables",
  "type": "object",
  "properties": {
    "CHOCO_INSTALL_METHOD": {
      "description": "How to install CHOCO. 'libscript_native' uses isolated version dirs, 'system' uses OS package manager, 'mise', 'asdf', 'pkgx', or 'vfox' defers to third-party tools.",
      "type": "string",
      "default": "libscript_native"
    },
    "CHOCO_VERSION": {
      "description": "Specific version of choco to install.",
      "type": "string",
      "default": "latest"
    }
  },
  "description": "Configuration variables for choco"
}
