# google-cloud-sdk

Provides bootstrap definitions and wrappers for installing the Google Cloud SDK (`gcloud`).

## Configuration Options
See `vars.schema.json` for installation options and version pinning.
