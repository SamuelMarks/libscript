{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "/_lib/package-managers/nvm/vars.schema.json",
  "title": "Nvm variables",
  "description": "Configuration variables for nvm",
  "type": "object",
  "properties": {
    "NVM_INSTALL_METHOD": {
      "description": "How to install NVM. 'libscript_native' uses isolated version dirs, 'system' uses OS package manager, 'mise', 'asdf', 'pkgx', or 'vfox' defers to third-party tools.",
      "type": "string",
      "default": "libscript_native"
    },
    "NVM_VERSION": {
      "description": "Specific version of nvm to install.",
      "type": "string",
      "default": "latest"
    }
  }
}
