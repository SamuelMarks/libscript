{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "/_lib/package-managers/npm/vars.schema.json",
  "title": "Npm variables",
  "description": "Configuration variables for npm",
  "type": "object",
  "properties": {
    "NPM_INSTALL_METHOD": {
      "description": "How to install NPM. 'libscript_native' uses isolated version dirs, 'system' uses OS package manager, 'mise', 'asdf', 'pkgx', or 'vfox' defers to third-party tools.",
      "type": "string",
      "default": "libscript_native"
    },
    "NPM_VERSION": {
      "description": "Specific version of npm to install.",
      "type": "string",
      "default": "latest"
    }
  }
}
