{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "/_lib/package-managers/nuget/vars.schema.json",
  "title": "Nuget variables",
  "description": "Configuration variables for nuget",
  "type": "object",
  "properties": {
    "NUGET_INSTALL_METHOD": {
      "description": "How to install NUGET. 'libscript_native' uses isolated version dirs, 'system' uses OS package manager, 'mise', 'asdf', 'pkgx', or 'vfox' defers to third-party tools.",
      "type": "string",
      "default": "libscript_native"
    },
    "NUGET_VERSION": {
      "description": "Specific version of nuget to install.",
      "type": "string",
      "default": "latest"
    }
  }
}
