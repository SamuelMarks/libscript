{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "/_lib/package-managers/snap/vars.schema.json",
  "title": "Snap variables",
  "description": "Configuration variables for snap",
  "type": "object",
  "properties": {
    "SNAP_INSTALL_METHOD": {
      "description": "How to install SNAP. 'libscript_native' uses isolated version dirs, 'system' uses OS package manager, 'mise', 'asdf', 'pkgx', or 'vfox' defers to third-party tools.",
      "type": "string",
      "default": "libscript_native"
    },
    "SNAP_VERSION": {
      "description": "Specific version of snap to install.",
      "type": "string",
      "default": "latest"
    }
  }
}
