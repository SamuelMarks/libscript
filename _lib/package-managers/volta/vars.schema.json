{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "/_lib/package-managers/volta/vars.schema.json",
  "title": "Volta variables",
  "description": "Configuration variables for volta",
  "type": "object",
  "properties": {
    "VOLTA_HOME": {
      "type": "string",
      "description": "Custom installation directory for Volta. Defaults to ~/.volta."
    },
    "VOLTA_FEATURE_PNPM": {
      "type": "string",
      "enum": [
        "1",
        "0"
      ],
      "description": "Set to 1 to enable Volta's experimental pnpm support."
    },
    "VOLTA_VERSION": {
      "description": "Specific version of volta to install.",
      "type": "string",
      "default": "latest"
    }
  }
}
