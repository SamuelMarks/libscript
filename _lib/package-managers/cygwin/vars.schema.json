{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "/_lib/package-managers/cygwin/vars.schema.json",
  "title": "Cygwin variables",
  "description": "Configuration variables for cygwin",
  "type": "object",
  "properties": {
    "CYGWIN_INSTALL_METHOD": {
      "description": "How to install CYGWIN. 'libscript_native' uses isolated version dirs, 'system' uses OS package manager, 'mise', 'asdf', 'pkgx', or 'vfox' defers to third-party tools.",
      "type": "string",
      "default": "libscript_native"
    },
    "CYGWIN_VERSION": {
      "description": "Specific version of cygwin to install.",
      "type": "string",
      "default": "latest"
    }
  }
}
