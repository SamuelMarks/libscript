{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "/_lib/package-managers/gem/vars.schema.json",
  "title": "Gem variables",
  "description": "Configuration variables for gem",
  "type": "object",
  "properties": {
    "GEM_INSTALL_METHOD": {
      "description": "How to install GEM. 'libscript_native' uses isolated version dirs, 'system' uses OS package manager, 'mise', 'asdf', 'pkgx', or 'vfox' defers to third-party tools.",
      "type": "string",
      "default": "libscript_native"
    },
    "GEM_VERSION": {
      "description": "Specific version of gem to install.",
      "type": "string",
      "default": "latest"
    }
  }
}
