{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "/_lib/package-managers/pyenv/vars.schema.json",
  "title": "Pyenv variables",
  "description": "Configuration variables for pyenv",
  "type": "object",
  "properties": {
    "PYENV_INSTALL_METHOD": {
      "description": "How to install PYENV. 'libscript_native' uses isolated version dirs, 'system' uses OS package manager, 'mise', 'asdf', 'pkgx', or 'vfox' defers to third-party tools.",
      "type": "string",
      "default": "libscript_native"
    },
    "PYENV_VERSION": {
      "description": "Specific version of pyenv to install.",
      "type": "string",
      "default": "latest"
    }
  }
}
