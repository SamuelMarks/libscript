{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "/_lib/package-managers/emerge/vars.schema.json",
  "title": "Emerge variables",
  "description": "Configuration variables for emerge",
  "type": "object",
  "properties": {
    "EMERGE_INSTALL_METHOD": {
      "description": "How to install EMERGE. 'libscript_native' uses isolated version dirs, 'system' uses OS package manager, 'mise', 'asdf', 'pkgx', or 'vfox' defers to third-party tools.",
      "type": "string",
      "default": "libscript_native"
    },
    "EMERGE_VERSION": {
      "description": "Specific version of emerge to install.",
      "type": "string",
      "default": "latest"
    }
  }
}
