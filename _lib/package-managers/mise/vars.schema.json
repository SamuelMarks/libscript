{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "/_lib/package-managers/mise/vars.schema.json",
  "title": "Mise variables",
  "description": "Configuration variables for mise",
  "type": "object",
  "properties": {
    "MISE_INSTALL_METHOD": {
      "description": "How to install MISE. 'libscript_native' uses isolated version dirs, 'system' uses OS package manager, 'mise', 'asdf', 'pkgx', or 'vfox' defers to third-party tools.",
      "type": "string",
      "default": "libscript_native"
    },
    "MISE_VERSION": {
      "description": "Specific version of mise to install.",
      "type": "string",
      "default": "latest"
    }
  }
}
