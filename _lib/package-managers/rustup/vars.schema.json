{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "/_lib/package-managers/rustup/vars.schema.json",
  "title": "Rustup variables",
  "description": "Configuration variables for rustup",
  "type": "object",
  "properties": {
    "RUSTUP_INSTALL_METHOD": {
      "description": "How to install RUSTUP. 'libscript_native' uses isolated version dirs, 'system' uses OS package manager, 'mise', 'asdf', 'pkgx', or 'vfox' defers to third-party tools.",
      "type": "string",
      "default": "libscript_native"
    },
    "RUSTUP_VERSION": {
      "description": "Specific version of rustup to install.",
      "type": "string",
      "default": "latest"
    }
  }
}
