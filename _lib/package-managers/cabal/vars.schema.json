{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "/_lib/package-managers/cabal/vars.schema.json",
  "title": "Cabal variables",
  "description": "Configuration variables for cabal",
  "type": "object",
  "properties": {
    "CABAL_INSTALL_METHOD": {
      "description": "How to install CABAL. 'libscript_native' uses isolated version dirs, 'system' uses OS package manager, 'mise', 'asdf', 'pkgx', or 'vfox' defers to third-party tools.",
      "type": "string",
      "default": "libscript_native"
    },
    "CABAL_VERSION": {
      "description": "Specific version of cabal to install.",
      "type": "string",
      "default": "latest"
    }
  }
}
