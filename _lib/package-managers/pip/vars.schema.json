{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "/_lib/package-managers/pip/vars.schema.json",
  "title": "Pip variables",
  "description": "Configuration variables for pip",
  "type": "object",
  "properties": {
    "PIP_INSTALL_METHOD": {
      "description": "How to install PIP. 'libscript_native' uses isolated version dirs, 'system' uses OS package manager, 'mise', 'asdf', 'pkgx', or 'vfox' defers to third-party tools.",
      "type": "string",
      "default": "libscript_native"
    },
    "PIP_VERSION": {
      "description": "Specific version of pip to install.",
      "type": "string",
      "default": "latest"
    }
  }
}
