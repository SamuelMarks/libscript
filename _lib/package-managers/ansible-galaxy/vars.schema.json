{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "/_lib/package-managers/ansible-galaxy/vars.schema.json",
  "title": "Ansible Galaxy variables",
  "description": "Configuration variables for ansible-galaxy",
  "type": "object",
  "properties": {
    "ANSIBLE_GALAXY_VERSION": {
      "description": "Specific version of ansible-galaxy to install.",
      "type": "string",
      "default": "latest"
    },
    "ANSIBLE_GALAXY_INSTALL_METHOD": {
      "description": "How to install ANSIBLE GALAXY. 'libscript_native' uses isolated version dirs, 'system' uses OS package manager, 'mise', 'asdf', 'pkgx', or 'vfox' defers to third-party tools.",
      "type": "string",
      "enum": ["libscript_native", "system", "mise", "asdf", "pkgx", "vfox"],
      "default": "libscript_native"
    }
  }
}
