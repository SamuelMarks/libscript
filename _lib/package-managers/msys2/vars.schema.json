{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "/_lib/package-managers/msys2/vars.schema.json",
  "title": "Msys2 variables",
  "type": "object",
  "properties": {
    "MSYS2_INSTALL_METHOD": {
      "description": "How to install MSYS2. 'libscript_native' uses isolated version dirs, 'system' uses OS package manager, 'mise', 'asdf', 'pkgx', or 'vfox' defers to third-party tools.",
      "type": "string",
      "default": "libscript_native"
    },
    "MSYS2_VERSION": {
      "description": "Specific version of msys2 to install.",
      "type": "string",
      "default": "latest"
    }
  },
  "description": "Configuration variables for msys2"
}
