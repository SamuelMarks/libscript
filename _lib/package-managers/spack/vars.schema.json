{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "/_lib/package-managers/spack/vars.schema.json",
  "title": "Spack variables",
  "description": "Configuration variables for spack",
  "type": "object",
  "properties": {
    "SPACK_INSTALL_METHOD": {
      "description": "How to install SPACK. 'libscript_native' uses isolated version dirs, 'system' uses OS package manager, 'mise', 'asdf', 'pkgx', or 'vfox' defers to third-party tools.",
      "type": "string",
      "default": "libscript_native"
    },
    "SPACK_VERSION": {
      "description": "Specific version of spack to install.",
      "type": "string",
      "default": "latest"
    }
  }
}
