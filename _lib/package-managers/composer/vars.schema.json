{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "/_lib/package-managers/composer/vars.schema.json",
  "title": "Composer variables",
  "description": "Configuration variables for composer",
  "type": "object",
  "properties": {
    "COMPOSER_INSTALL_METHOD": {
      "description": "How to install COMPOSER. 'libscript_native' uses isolated version dirs, 'system' uses OS package manager, 'mise', 'asdf', 'pkgx', or 'vfox' defers to third-party tools.",
      "type": "string",
      "default": "libscript_native"
    },
    "COMPOSER_VERSION": {
      "description": "Specific version of composer to install.",
      "type": "string",
      "default": "latest"
    }
  }
}
