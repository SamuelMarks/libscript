{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "/_lib/package-managers/bundler/vars.schema.json",
  "title": "Bundler variables",
  "description": "Configuration variables for bundler",
  "type": "object",
  "properties": {
    "BUNDLER_INSTALL_METHOD": {
      "description": "How to install BUNDLER. 'libscript_native' uses isolated version dirs, 'system' uses OS package manager, 'mise', 'asdf', 'pkgx', or 'vfox' defers to third-party tools.",
      "type": "string",
      "default": "libscript_native"
    },
    "BUNDLER_VERSION": {
      "description": "Specific version of bundler to install.",
      "type": "string",
      "default": "latest"
    }
  }
}
