{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "/_lib/web-servers/httpd/vars.schema.json",
  "title": "httpd vars",
  "description": "Variables for Apache HTTPD",
  "type": "object",
  "properties": {
    "HTTPD_VERSION": {
      "description": "Specific version of httpd to install. Can be a numeric version or an alias.",
      "type": "string",
      "default": "latest",
      "versionAliases": [
        "latest",
        "stable"
      ]
    },
    "HTTPD_INSTALL_METHOD": {
      "description": "How to install HTTPD. 'system' uses the native OS package manager, 'source' compiles/downloads binaries.",
      "type": "string",
      "enum": [
        "system",
        "source"
      ],
      "default": "system"
    },
    "LIBSCRIPT_LISTEN_PORT": {
      "type": [
        "string",
        "integer"
      ],
      "description": "Global port to listen on"
    },
    "LIBSCRIPT_LISTEN_ADDRESS": {
      "type": "string",
      "description": "Global address to listen on"
    },
    "LIBSCRIPT_LISTEN_SOCKET": {
      "type": "string",
      "description": "Global unix socket to listen on"
    },
    "HTTPD_LISTEN_PORT": {
      "type": [
        "string",
        "integer"
      ],
      "description": "Port for HTTPD to listen on"
    },
    "HTTPD_LISTEN_ADDRESS": {
      "type": "string",
      "description": "Address for HTTPD to listen on"
    },
    "HTTPD_LISTEN_SOCKET": {
      "type": "string",
      "description": "Unix socket for HTTPD to listen on"
    },
    "WWWROOT_NAME": {
      "description": "The primary domain or server name (ServerName) for Apache to bind to.",
      "type": "string"
    },
    "WWWROOT_PATH": {
      "description": "The absolute path to the DocumentRoot directory.",
      "type": "string"
    },
    "WWWROOT_LISTEN": {
      "description": "The HTTP port Apache should listen on (defaults to 80).",
      "type": "string",
      "default": "80"
    },
    "HTTPD_SERVICE_RUN_AS_USER": {
      "description": "Windows local user account to run the Apache service under (leave empty for default).",
      "type": "string"
    },
    "HTTPD_SERVICE_RUN_AS_PASSWORD": {
      "description": "Password for the Windows local user account (if applicable).",
      "type": "string"
    },
    "HTTPD_SERVICE_NAME": {
      "description": "Custom name for the Windows Service (allows side-by-side installations).",
      "type": "string",
      "default": "libscript_httpd"
    },
    "LISTEN": {
      "type": "string",
      "description": "Address/port to listen on."
    },
    "WWWROOT": {
      "type": "string",
      "description": "Web server root directory."
    },
    "PHP_FPM_LISTEN": {
      "type": "string",
      "description": "Listen address/socket for PHP-FPM."
    },
    "SERVER_NAME": {
      "type": "string",
      "description": "Name of the server (e.g., example.com)."
    }
  }
}
