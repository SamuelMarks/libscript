{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "/_lib/web-servers/lighttpd/vars.schema.json",
  "title": "lighttpd vars",
  "description": "Variables for Lighttpd",
  "type": "object",
  "properties": {
    "LIGHTTPD_INSTALL_METHOD": {
      "description": "Installation method for lighttpd (libscript_native, system, mise, asdf, pkgx, vfox).",
      "type": "string",
      "default": "libscript_native"
    },
    "LIGHTTPD_VERSION": {
      "description": "Specific version of lighttpd to install. Can be a numeric version or an alias.",
      "type": "string",
      "default": "latest",
      "version_aliases": ["latest", "stable"]
    },
    "LIGHTTPD_INSTALL_METHOD": {
      "description": "How to install LIGHTTPD. 'libscript_native' uses isolated version dirs, 'system' uses OS package manager, 'mise', 'asdf', 'pkgx', or 'vfox' defers to third-party tools.",
      "type": "string",
      "enum": ["libscript_native", "system", "mise", "asdf", "pkgx", "vfox"],
      "default": "libscript_native"
    },
    "LIBSCRIPT_LISTEN_PORT": {
      "type": ["string", "integer"],
      "description": "Global port to listen on"
    },
    "LIBSCRIPT_LISTEN_ADDRESS": {
      "type": "string",
      "description": "Global address to listen on"
    },
    "LIBSCRIPT_LISTEN_SOCKET": {
      "type": "string",
      "description": "Global unix socket to listen on"
    },
    "LIGHTTPD_LISTEN_PORT": {
      "type": ["string", "integer"],
      "description": "Port for LIGHTTPD to listen on"
    },
    "LIGHTTPD_LISTEN_ADDRESS": {
      "type": "string",
      "description": "Address for LIGHTTPD to listen on"
    },
    "LIGHTTPD_LISTEN_SOCKET": {
      "type": "string",
      "description": "Unix socket for LIGHTTPD to listen on"
    }
  }
}
