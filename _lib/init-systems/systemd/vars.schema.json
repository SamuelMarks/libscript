{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "/_lib/init-systems/systemd/vars.schema.json",
  "title": "systemd daemon vars",
  "description": "Variables to configure systemd services",
  "type": "object",
  "properties": {
    "SYSTEMD_EXEC_START": {
      "description": "Executor",
      "type": "string"
    },
    "SYSTEMD_WORKING_DIR": {
      "description": "Working directory that `EXEC_START` will be run from",
      "type": "string"
    },
    "SYSTEMD_ENV": {
      "description": "Optional additional properties as key/value pairs",
      "type": "string"
    },
    "SYSTEMD_INSTALL_METHOD": {
      "description": "How to install SYSTEMD. 'libscript_native' uses isolated version dirs, 'system' uses OS package manager, 'mise', 'asdf', 'pkgx', or 'vfox' defers to third-party tools.",
      "type": "string",
      "enum": ["libscript_native", "system", "mise", "asdf", "pkgx", "vfox"],
      "default": "libscript_native"
    }
  }
}
