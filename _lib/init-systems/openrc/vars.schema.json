{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "/_lib/init-systems/openrc/vars.schema.json",
  "title": "openrc daemon vars",
  "description": "OpenRC init system.",
  "type": "object",
  "properties": {
    "OPENRC_INSTALL_METHOD": {
      "description": "How to install OPENRC. 'libscript_native' uses isolated version dirs, 'system' uses OS package manager, 'mise', 'asdf', 'pkgx', or 'vfox' defers to third-party tools.",
      "type": "string",
      "enum": ["libscript_native", "system", "mise", "asdf", "pkgx", "vfox"],
      "default": "libscript_native"
    }
  }
}
