{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "/_lib/databases/coursier/vars.schema.json",
  "title": "coursier storage vars",
  "description": "coursier storage vars that can be set",
  "type": "object",
  "properties": {
    "COURSIER_VERSION": {
      "description": "Specific version of coursier to install. Can be a numeric version or an alias.",
      "type": "string",
      "default": "latest",
      "versionAliases": [
        "latest",
        "stable"
      ]
    },
    "LIBSCRIPT_GLOBAL_INSTALL_METHOD": {
      "description": "Global override for how software should be installed across all systems (e.g. system package manager vs downloaded binaries/from-source).",
      "type": "string",
      "enum": [
        "system",
        "source"
      ],
      "default": "source"
    },
    "LIBSCRIPT_WINDOWS_PKG_MGR": {
      "description": "Global package manager override for Windows targets (e.g. winget, choco).",
      "type": "string",
      "enum": [
        "winget",
        "choco"
      ],
      "default": "winget"
    },
    "COURSIER_INSTALL_METHOD": {
      "description": "How to install COURSIER. 'system' uses the native OS package manager, 'source' compiles/downloads binaries.",
      "type": "string",
      "enum": [
        "system",
        "source"
      ],
      "default": "source"
    },
    "LIBSCRIPT_LISTEN_PORT": {
      "type": [
        "string",
        "integer"
      ],
      "description": "Global port to listen on"
    },
    "LIBSCRIPT_LISTEN_ADDRESS": {
      "type": "string",
      "description": "Global address to listen on"
    },
    "LIBSCRIPT_LISTEN_SOCKET": {
      "type": "string",
      "description": "Global unix socket to listen on"
    },
    "COURSIER_LISTEN_PORT": {
      "type": [
        "string",
        "integer"
      ],
      "description": "Port for COURSIER to listen on"
    },
    "COURSIER_LISTEN_ADDRESS": {
      "type": "string",
      "description": "Address for COURSIER to listen on"
    },
    "COURSIER_LISTEN_SOCKET": {
      "type": "string",
      "description": "Unix socket for COURSIER to listen on"
    },
    "LIBSCRIPT_SERVICE_NAME": {
      "type": "string",
      "description": "Overrides the default service name."
    },
    "FORMAT": {
      "type": "string",
      "description": "Output format (e.g., json, text)."
    },
    "LIBSCRIPT_LOG_DRIVER": {
      "type": "string",
      "description": "Logging driver to use (e.g., fluentd)."
    },
    "LOGS_DIR": {
      "type": "string",
      "description": "Directory where logs should be stored."
    },
    "VAULT_TOKEN": {
      "type": "string",
      "description": "Token for HashiCorp Vault authentication."
    },
    "PREFIX": {
      "type": "string",
      "description": "Installation prefix."
    },
    "SERVE_FROM": {
      "type": "string",
      "description": "Base directory or context path from which the service should be served."
    },
    "LIBSCRIPT_LOG_HOST": {
      "type": "string",
      "description": "Host for remote logging."
    },
    "LIBSCRIPT_VERSION": {
      "type": "string",
      "description": "Specifies the version of the package to use."
    },
    "LIBSCRIPT_LOG_PORT": {
      "type": "string",
      "description": "Port for remote logging."
    }
  }
}