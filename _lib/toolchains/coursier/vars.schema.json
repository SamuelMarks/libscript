{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "/_lib/toolchains/coursier/vars.schema.json",
  "title": "Coursier variables",
  "description": "Configuration variables for Coursier",
  "type": "object",
  "properties": {
    "COURSIER_VERSION": {
      "description": "Specific version of coursier to install. Can be a numeric version or an alias.",
      "type": "string",
      "default": "latest",
      "version_aliases": ["latest", "stable"]
    },
    "COURSIER_INSTALL_METHOD": {
      "description": "How to install COURSIER. 'libscript-native' uses isolated version dirs, 'system' uses OS package manager, 'mise' or 'asdf' defers to third-party tools.",
      "type": "string",
      "enum": ["libscript-native", "system", "mise", "asdf"],
      "default": "libscript-native"
    },
    "LIBSCRIPT_LISTEN_PORT": {
      "type": ["string", "integer"],
      "description": "Global port to listen on"
    },
    "LIBSCRIPT_LISTEN_ADDRESS": {
      "type": "string",
      "description": "Global address to listen on"
    },
    "LIBSCRIPT_LISTEN_SOCKET": {
      "type": "string",
      "description": "Global unix socket to listen on"
    },
    "COURSIER_LISTEN_PORT": {
      "type": ["string", "integer"],
      "description": "Port for COURSIER to listen on"
    },
    "COURSIER_LISTEN_ADDRESS": {
      "type": "string",
      "description": "Address for COURSIER to listen on"
    },
    "COURSIER_LISTEN_SOCKET": {
      "type": "string",
      "description": "Unix socket for COURSIER to listen on"
    }
  }
}
