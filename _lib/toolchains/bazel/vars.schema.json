{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "/_lib/toolchains/bazel/vars.schema.json",
  "title": "Bazel variables",
  "description": "Configuration variables for Bazel",
  "type": "object",
  "properties": {
    "BAZEL_INSTALL_METHOD": {
      "description": "Installation method for bazel (libscript_native, system, mise, asdf, pkgx, vfox).",
      "type": "string",
      "default": "libscript_native"
    },
    "BAZEL_VERSION": {
      "description": "Specific version of bazel to install. Can be a numeric version or an alias.",
      "type": "string",
      "default": "latest",
      "version_aliases": ["latest", "stable"]
    },
    "BAZEL_INSTALL_METHOD": {
      "description": "How to install BAZEL. 'libscript_native' uses isolated version dirs, 'system' uses OS package manager, 'mise', 'asdf', 'pkgx', or 'vfox' defers to third-party tools.",
      "type": "string",
      "enum": ["libscript_native", "system", "mise", "asdf", "pkgx", "vfox"],
      "default": "libscript_native"
    },
    "LIBSCRIPT_LISTEN_PORT": {
      "type": ["string", "integer"],
      "description": "Global port to listen on"
    },
    "LIBSCRIPT_LISTEN_ADDRESS": {
      "type": "string",
      "description": "Global address to listen on"
    },
    "LIBSCRIPT_LISTEN_SOCKET": {
      "type": "string",
      "description": "Global unix socket to listen on"
    },
    "BAZEL_LISTEN_PORT": {
      "type": ["string", "integer"],
      "description": "Port for BAZEL to listen on"
    },
    "BAZEL_LISTEN_ADDRESS": {
      "type": "string",
      "description": "Address for BAZEL to listen on"
    },
    "BAZEL_LISTEN_SOCKET": {
      "type": "string",
      "description": "Unix socket for BAZEL to listen on"
    }
  }
}
