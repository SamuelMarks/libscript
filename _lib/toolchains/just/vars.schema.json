{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "/_lib/toolchains/just/vars.schema.json",
  "title": "Just variables",
  "description": "Configuration variables for Just",
  "type": "object",
  "properties": {
    "JUST_VERSION": {
      "description": "Specific version of just to install. Can be a numeric version or an alias.",
      "type": "string",
      "default": "latest"
    },
    "JUST_INSTALL_METHOD": {
      "description": "How to install JUST. 'system' uses the native OS package manager, 'libscript-native' downloads binaries natively.",
      "type": "string",
      "enum": ["system", "libscript-native", "mise", "asdf"],
      "default": "libscript-native"
    }
  }
}
