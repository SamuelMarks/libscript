{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "/_lib/toolchains/just/vars.schema.json",
  "title": "Just variables",
  "description": "Configuration variables for Just",
  "type": "object",
  "properties": {
    "JUST_INSTALL_METHOD": {
      "description": "How to install JUST. 'libscript_native' uses isolated version dirs, 'system' uses OS package manager, 'mise', 'asdf', 'pkgx', or 'vfox' defers to third-party tools.",
      "type": "string",
      "enum": ["libscript_native", "system", "mise", "asdf", "pkgx", "vfox"],
      "default": "libscript_native"
    },
    "JUST_VERSION": {
      "description": "Specific version of just to install. Can be a numeric version or an alias.",
      "type": "string",
      "default": "latest"
    }
  }
}
