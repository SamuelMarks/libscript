{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "/_lib/toolchains/xpk/vars.schema.json",
  "title": "xpk variables",
  "description": "Configuration variables for xpk",
  "type": "object",
  "properties": {
    "XPK_VERSION": {
      "description": "Specific version of xpk to install. Can be a numeric version or an alias.",
      "type": "string",
      "default": "latest",
      "version_aliases": ["latest", "stable"]
    },
    "XPK_INSTALL_METHOD": {
      "description": "How to install XPK. 'system' delegates to package manager.",
      "type": "string",
      "enum": ["system", "libscript-native", "mise", "asdf"],
      "default": "system"
    },
    "LIBSCRIPT_LISTEN_PORT": {
      "type": ["string", "integer"],
      "description": "Global port to listen on"
    },
    "LIBSCRIPT_LISTEN_ADDRESS": {
      "type": "string",
      "description": "Global address to listen on"
    },
    "LIBSCRIPT_LISTEN_SOCKET": {
      "type": "string",
      "description": "Global unix socket to listen on"
    },
    "XPK_LISTEN_PORT": {
      "type": ["string", "integer"],
      "description": "Port for XPK to listen on"
    },
    "XPK_LISTEN_ADDRESS": {
      "type": "string",
      "description": "Address for XPK to listen on"
    },
    "XPK_LISTEN_SOCKET": {
      "type": "string",
      "description": "Unix socket for XPK to listen on"
    }
  }
}
