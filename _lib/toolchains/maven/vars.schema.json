{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "/_lib/toolchains/maven/vars.schema.json",
  "title": "Maven variables",
  "description": "Configuration variables for Maven",
  "type": "object",
  "properties": {
    "MAVEN_VERSION": {
      "description": "Specific version of maven to install. Can be a numeric version or an alias.",
      "type": "string",
      "default": "latest"
    },
    "MAVEN_INSTALL_METHOD": {
      "description": "How to install MAVEN. 'libscript-native' uses isolated version dirs, 'system' uses OS package manager, 'mise' or 'asdf' defers to third-party tools.",
      "type": "string",
      "enum": ["libscript-native", "system", "mise", "asdf"],
      "default": "libscript-native"
    }
  }
}
