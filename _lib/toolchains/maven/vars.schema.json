{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "/_lib/toolchains/maven/vars.schema.json",
  "title": "Maven variables",
  "description": "Configuration variables for Maven",
  "type": "object",
  "properties": {
    "MAVEN_INSTALL_METHOD": {
      "description": "Installation method for maven (libscript_native, system, mise, asdf, pkgx, vfox).",
      "type": "string",
      "default": "libscript_native"
    },
    "MAVEN_VERSION": {
      "description": "Specific version of maven to install. Can be a numeric version or an alias.",
      "type": "string",
      "default": "latest"
    },
    "MAVEN_INSTALL_METHOD": {
      "description": "How to install MAVEN. 'libscript_native' uses isolated version dirs, 'system' uses OS package manager, 'mise', 'asdf', 'pkgx', or 'vfox' defers to third-party tools.",
      "type": "string",
      "enum": ["libscript_native", "system", "mise", "asdf", "pkgx", "vfox"],
      "default": "libscript_native"
    }
  }
}
