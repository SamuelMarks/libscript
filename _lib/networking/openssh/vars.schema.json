{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "/_lib/networking/openssh/vars.schema.json",
  "title": "OpenSSH variables",
  "description": "Configuration variables for OpenSSH",
  "type": "object",
  "properties": {
    "OPENSSH_VERSION": {
      "description": "Specific version of OpenSSH to install.",
      "type": "string",
      "default": "latest"
    },
    "OPENSSH_INSTALL_METHOD": {
      "type": "string",
      "description": "How to install OPENSSH. 'libscript_native' uses isolated version dirs, 'system' uses OS package manager.",
      "default": "system"
    }
  }
}
