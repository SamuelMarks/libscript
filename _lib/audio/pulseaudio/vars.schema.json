{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "/_lib/audio/pulseaudio/vars.schema.json",
  "title": "PulseAudio variables",
  "description": "Configuration variables for PulseAudio",
  "type": "object",
  "properties": {
    "PULSEAUDIO_VERSION": {
      "description": "Specific version of pulseaudio to install.",
      "type": "string",
      "default": "latest"
    },
    "PULSEAUDIO_INSTALL_METHOD": {
      "type": "string",
      "description": "How to install PULSEAUDIO. 'libscript_native' uses isolated version dirs, 'system' uses OS package manager.",
      "default": "system"
    }
  }
}
