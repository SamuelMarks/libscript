# pacman

Bootstrap module for the `pacman` package manager.

## Overview

This module ensures `pacman` is available and updated. Since `pacman` is natively integrated into its corresponding Linux distributions, this typically just ensures the package index is up-to-date.

## OS Compatibility

- **Linux**: Native to specific distributions (e.g., Arch Linux/MSYS2).
- **macOS**: Not applicable.
- **Windows**: Supported via MSYS2.

## Variables

See `vars.schema.json` for details on available variables.
