{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "/_lib/message-brokers/mosquitto/vars.schema.json",
  "title": "mosquitto vars",
  "description": "Variables for Mosquitto",
  "type": "object",
  "properties": {
    "MOSQUITTO_VERSION": {
      "description": "Specific version of mosquitto to install. Can be a numeric version or an alias.",
      "type": "string",
      "default": "latest",
      "versionAliases": [
        "latest",
        "stable"
      ]
    },
    "MOSQUITTO_INSTALL_METHOD": {
      "description": "How to install MOSQUITTO. 'system' uses the native OS package manager, 'source' compiles/downloads binaries.",
      "type": "string",
      "enum": [
        "system",
        "source"
      ],
      "default": "source"
    },
    "LIBSCRIPT_LISTEN_PORT": {
      "type": [
        "string",
        "integer"
      ],
      "description": "Global port to listen on"
    },
    "LIBSCRIPT_LISTEN_ADDRESS": {
      "type": "string",
      "description": "Global address to listen on"
    },
    "LIBSCRIPT_LISTEN_SOCKET": {
      "type": "string",
      "description": "Global unix socket to listen on"
    },
    "MOSQUITTO_LISTEN_PORT": {
      "type": [
        "string",
        "integer"
      ],
      "description": "Port for MOSQUITTO to listen on"
    },
    "MOSQUITTO_LISTEN_ADDRESS": {
      "type": "string",
      "description": "Address for MOSQUITTO to listen on"
    },
    "MOSQUITTO_LISTEN_SOCKET": {
      "type": "string",
      "description": "Unix socket for MOSQUITTO to listen on"
    }
  }
}
