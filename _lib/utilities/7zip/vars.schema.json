{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "/_lib/utilities/7zip/vars.schema.json",
  "title": "7-Zip variables",
  "description": "Configuration variables for 7-Zip",
  "type": "object",
  "properties": {
    "SEVENZIP_VERSION": {
      "description": "Specific version of 7zip to install.",
      "type": "string",
      "default": "latest"
    },
    "SEVENZIP_INSTALL_METHOD": {
      "type": "string",
      "description": "How to install SEVENZIP. 'libscript_native' uses isolated version dirs, 'system' uses OS package manager, 'mise', 'asdf', 'pkgx', or 'vfox' defers to third-party tools.",
      "default": "system"
    }
  }
}
