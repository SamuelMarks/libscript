{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "/_lib/utilities/busybox/vars.schema.json",
  "title": "BusyBox variables",
  "description": "Configuration variables for BusyBox",
  "type": "object",
  "properties": {
    "BUSYBOX_INSTALL_METHOD": {
      "type": "string",
      "description": "How to install BUSYBOX. 'libscript_native' uses isolated version dirs, 'system' uses OS package manager, 'mise', 'asdf', 'pkgx', or 'vfox' defers to third-party tools.",
      "default": "system"
    },
    "BUSYBOX_VERSION": {
      "description": "Specific version of busybox to install.",
      "type": "string",
      "default": "latest"
    }
  }
}
