{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "/_lib/utilities/tmux/vars.schema.json",
  "title": "Tmux variables",
  "description": "Configuration variables for tmux",
  "type": "object",
  "properties": {
    "TMUX_INSTALL_METHOD": {
      "description": "How to install TMUX. 'libscript_native' uses isolated version dirs, 'system' uses OS package manager, 'mise', 'asdf', 'pkgx', or 'vfox' defers to third-party tools.",
      "type": "string",
      "default": "libscript_native"
    },
    "TMUX_VERSION": {
      "description": "Specific version of tmux to install.",
      "type": "string",
      "default": "latest"
    }
  }
}
