{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "/_lib/utilities/powershell/vars.schema.json",
  "title": "PowerShell variables",
  "description": "Configuration variables for PowerShell",
  "type": "object",
  "properties": {
    "POWERSHELL_INSTALL_METHOD": {
      "description": "How to install POWERSHELL. 'libscript_native' uses isolated version dirs, 'system' uses OS package manager, 'mise', 'asdf', 'pkgx', or 'vfox' defers to third-party tools.",
      "type": "string",
      "default": "system"
    },
    "POWERSHELL_VERSION": {
      "description": "Specific version of PowerShell to install.",
      "type": "string",
      "default": "latest"
    },
    "POWERSHELL_INSTALL_METHOD": {
      "type": "string",
      "description": "How to install POWERSHELL. 'libscript_native' uses isolated version dirs, 'system' uses OS package manager, 'mise', 'asdf', 'pkgx', or 'vfox' defers to third-party tools.",
      "default": "system"
    }
  }
}
