{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "/_lib/utilities/curl/vars.schema.json",
  "title": "curl variables",
  "description": "Configuration variables for curl",
  "type": "object",
  "properties": {
    "CURL_INSTALL_METHOD": {
      "description": "How to install CURL. 'libscript_native' uses isolated version dirs, 'system' uses OS package manager, 'mise', 'asdf', 'pkgx', or 'vfox' defers to third-party tools.",
      "type": "string",
      "default": "libscript_native"
    },
    "CURL_VERSION": {
      "description": "Specific version of curl to install.",
      "type": "string",
      "default": "latest"
    }
  }
}
