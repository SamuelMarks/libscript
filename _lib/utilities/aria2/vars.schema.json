{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "/_lib/utilities/aria2/vars.schema.json",
  "title": "aria2 variables",
  "description": "Configuration variables for aria2",
  "type": "object",
  "properties": {
    "ARIA2_INSTALL_METHOD": {
      "type": "string",
      "description": "How to install ARIA2. 'libscript_native' uses isolated version dirs, 'system' uses OS package manager, 'mise', 'asdf', 'pkgx', or 'vfox' defers to third-party tools.",
      "default": "system"
    },
    "ARIA2_VERSION": {
      "description": "Specific version of aria2 to install.",
      "type": "string",
      "default": "latest"
    }
  }
}
