{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "/_lib/utilities/jq/vars.schema.json",
  "title": "jq variables",
  "description": "Configuration variables for jq",
  "type": "object",
  "properties": {
    "JQ_INSTALL_METHOD": {
      "description": "How to install JQ. 'libscript_native' uses isolated version dirs, 'system' uses OS package manager, 'mise', 'asdf', 'pkgx', or 'vfox' defers to third-party tools.",
      "type": "string",
      "default": "libscript_native"
    },
    "JQ_VERSION": {
      "description": "Specific version of jq to install.",
      "type": "string",
      "default": "latest"
    },
    "JQ_INSTALL_METHOD": {
      "type": "string",
      "description": "How to install JQ. 'libscript_native' uses isolated version dirs, 'system' uses OS package manager, 'mise', 'asdf', 'pkgx', or 'vfox' defers to third-party tools.",
      "default": "libscript_native"
    }
  }
}
