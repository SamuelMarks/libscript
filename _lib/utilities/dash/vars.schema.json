{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "/_lib/utilities/dash/vars.schema.json",
  "title": "Dash variables",
  "description": "Configuration variables for Dash",
  "type": "object",
  "properties": {
    "DASH_INSTALL_METHOD": {
      "description": "How to install DASH. 'libscript_native' uses isolated version dirs, 'system' uses OS package manager, 'mise', 'asdf', 'pkgx', or 'vfox' defers to third-party tools.",
      "type": "string",
      "default": "libscript_native"
    },
    "DASH_VERSION": {
      "description": "Specific version of dash to install.",
      "type": "string",
      "default": "latest"
    },
    "DASH_INSTALL_METHOD": {
      "type": "string",
      "description": "How to install DASH. 'libscript_native' uses isolated version dirs, 'system' uses OS package manager, 'mise', 'asdf', 'pkgx', or 'vfox' defers to third-party tools.",
      "default": "libscript_native"
    }
  }
}
