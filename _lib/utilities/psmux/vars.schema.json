{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "/_lib/utilities/psmux/vars.schema.json",
  "title": "psmux vars",
  "description": "Variables to configure psmux",
  "type": "object",
  "properties": {
    "PSMUX_INSTALL_METHOD": {
      "type": "string",
      "description": "How to install PSMUX. 'libscript_native' uses isolated version dirs, 'system' uses OS package manager, 'mise', 'asdf', 'pkgx', or 'vfox' defers to third-party tools.",
      "default": "libscript_native"
    },
    "PSMUX_VERSION": {
      "type": "string",
      "description": "Version of psmux to install",
      "default": "v3.3.6"
    }
  }
}
