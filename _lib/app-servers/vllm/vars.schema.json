{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "/_lib/toolchains/huggingface_hub/vars.schema.json",
  "title": "huggingface_hub variables",
  "description": "Configuration variables for huggingface_hub",
  "type": "object",
  "properties": {
    "VLLM_VERSION": {
      "description": "Specific version of huggingface_hub to install. Can be a numeric version or an alias.",
      "type": "string",
      "default": "latest",
      "version_aliases": ["latest", "stable"]
    },
    "VLLM_INSTALL_METHOD": {
      "description": "How to install VLLM. 'system' delegates to package manager.",
      "type": "string",
      "enum": ["system", "libscript-native", "mise", "asdf"],
      "default": "system"
    },
    "LIBSCRIPT_LISTEN_PORT": {
      "type": ["string", "integer"],
      "description": "Global port to listen on"
    },
    "LIBSCRIPT_LISTEN_ADDRESS": {
      "type": "string",
      "description": "Global address to listen on"
    },
    "LIBSCRIPT_LISTEN_SOCKET": {
      "type": "string",
      "description": "Global unix socket to listen on"
    },
    "VLLM_LISTEN_PORT": {
      "type": ["string", "integer"],
      "description": "Port for VLLM to listen on"
    },
    "VLLM_LISTEN_ADDRESS": {
      "type": "string",
      "description": "Address for VLLM to listen on"
    },
    "VLLM_LISTEN_SOCKET": {
      "type": "string",
      "description": "Unix socket for VLLM to listen on"
    }
  }
}
