{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "/_lib/databases/sqlite/vars.schema.json",
  "title": "sqlite vars",
  "description": "Variables for SQLite",
  "type": "object",
  "properties": {
    "SQLITE_VERSION": {
      "description": "Specific version of sqlite to install. Can be a numeric version or an alias.",
      "type": "string",
      "default": "latest",
      "version_aliases": ["latest", "stable"]
    },
    "SQLITE_INSTALL_METHOD": {
      "description": "How to install SQLITE. 'libscript_native' uses isolated version dirs, 'system' uses OS package manager, 'mise', 'asdf', 'pkgx', or 'vfox' defers to third-party tools.",
      "type": "string",
      "enum": ["libscript_native", "system", "mise", "asdf", "pkgx", "vfox"],
      "default": "libscript_native"
    },
    "LIBSCRIPT_LISTEN_PORT": {
      "type": ["string", "integer"],
      "description": "Global port to listen on"
    },
    "LIBSCRIPT_LISTEN_ADDRESS": {
      "type": "string",
      "description": "Global address to listen on"
    },
    "LIBSCRIPT_LISTEN_SOCKET": {
      "type": "string",
      "description": "Global unix socket to listen on"
    },
    "SQLITE_LISTEN_PORT": {
      "type": ["string", "integer"],
      "description": "Port for SQLITE to listen on"
    },
    "SQLITE_LISTEN_ADDRESS": {
      "type": "string",
      "description": "Address for SQLITE to listen on"
    },
    "SQLITE_LISTEN_SOCKET": {
      "type": "string",
      "description": "Unix socket for SQLITE to listen on"
    }
  }
}
