{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "DuckDB variables",
  "description": "Variables for DuckDB",
  "type": "object",
  "properties": {
    "DUCKDB_INSTALL_METHOD": {
      "description": "How to install DUCKDB. 'libscript_native' uses isolated version dirs, 'system' uses OS package manager, 'mise', 'asdf', 'pkgx', or 'vfox' defers to third-party tools.",
      "type": "string",
      "enum": ["libscript_native", "system", "mise", "asdf", "pkgx", "vfox"],
      "default": "libscript_native"
    }
  }
}
