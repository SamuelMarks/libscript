{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "/_lib/toolchains/gpu-vm/vars.schema.json",
  "title": "Bazel variables",
  "description": "Configuration variables for Bazel",
  "type": "object",
  "properties": {
    "GPU_VM_VERSION": {
      "description": "Specific version of gpu-vm to install. Can be a numeric version or an alias.",
      "type": "string",
      "default": "latest",
      "version_aliases": ["latest", "stable"]
    },
    "GPU_VM_INSTALL_METHOD": {
      "description": "How to install GPU-VM. 'libscript-native' uses isolated version dirs, 'system' uses OS package manager, 'mise' or 'asdf' defers to third-party tools.",
      "type": "string",
      "enum": ["libscript-native", "system", "mise", "asdf"],
      "default": "libscript-native"
    },
    "LIBSCRIPT_LISTEN_PORT": {
      "type": ["string", "integer"],
      "description": "Global port to listen on"
    },
    "LIBSCRIPT_LISTEN_ADDRESS": {
      "type": "string",
      "description": "Global address to listen on"
    },
    "LIBSCRIPT_LISTEN_SOCKET": {
      "type": "string",
      "description": "Global unix socket to listen on"
    },
    "GPU_VM_LISTEN_PORT": {
      "type": ["string", "integer"],
      "description": "Port for GPU_VM to listen on"
    },
    "GPU_VM_LISTEN_ADDRESS": {
      "type": "string",
      "description": "Address for GPU_VM to listen on"
    },
    "GPU_VM_LISTEN_SOCKET": {
      "type": "string",
      "description": "Unix socket for GPU_VM to listen on"
    }
  }
}
