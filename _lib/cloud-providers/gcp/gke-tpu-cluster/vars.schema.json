{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "/_lib/cloud-providers/gcp/gke-tpu-cluster/vars.schema.json",
  "title": "GCP GKE TPU Cluster variables",
  "description": "Configuration variables for GCP GKE TPU Clusters",
  "type": "object",
  "properties": {
    "GKE_CLUSTER_VERSION": {
      "description": "Specific version of GKE to install or use for the cluster.",
      "type": "string",
      "default": "latest",
      "version_aliases": ["latest", "stable"]
    },
    "GKE_CLUSTER_INSTALL_METHOD": {
      "description": "How to install GKE Cluster tools.",
      "type": "string",
      "enum": ["libscript_native", "system", "mise", "asdf"],
      "default": "libscript_native"
    }
  }
}
