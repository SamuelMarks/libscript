{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "/_lib/languages/python/vars.schema.json",
  "title": "python vars",
  "description": "Variables for Python Runtime",
  "type": "object",
  "properties": {
    "PYTHON_VERSION": {
      "description": "Version of Python demanded. Can be a specific numeric version number or an alias",
      "type": "string",
      "default": "3.11",
      "versionAliases": [
        "latest",
        "cpython"
      ]
    },
    "VENV": {
      "description": "Path to a Python virtualenv (will create if nonexistent)",
      "type": "string"
    },
    "PYTHON_INSTALL_METHOD": {
      "description": "How to install PYTHON. 'system' uses the native OS package manager, 'source' compiles/downloads binaries.",
      "type": "string",
      "enum": [
        "system",
        "source"
      ],
      "default": "system"
    }
  }
}
