{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "/_lib/languages/elixir/vars.schema.json",
  "title": "elixir vars",
  "description": "Variables for Elixir Toolchain",
  "type": "object",
  "properties": {
    "ELIXIR_VERSION": {
      "description": "Specific version of elixir to install. Can be a numeric version or an alias.",
      "type": "string",
      "default": "latest",
      "versionAliases": [
        "latest",
        "stable"
      ]
    },
    "ELIXIR_INSTALL_METHOD": {
      "description": "How to install ELIXIR. 'system' uses the native OS package manager, 'source' compiles/downloads binaries.",
      "type": "string",
      "enum": [
        "system",
        "source"
      ],
      "default": "system"
    }
  }
}
