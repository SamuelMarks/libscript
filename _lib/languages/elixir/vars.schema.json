{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "/_lib/languages/elixir/vars.schema.json",
  "title": "elixir vars",
  "description": "Variables for Elixir Toolchain",
  "type": "object",
  "properties": {
    "ELIXIR_VERSION": {
      "description": "Specific version of elixir to install. Can be a numeric version or an alias.",
      "type": "string",
      "default": "latest",
      "version_aliases": ["latest", "stable"]
    },
    "ELIXIR_INSTALL_METHOD": {
      "description": "How to install ELIXIR. 'libscript-native' uses isolated version dirs, 'system' uses OS package manager, 'mise' or 'asdf' defers to third-party tools.",
      "type": "string",
      "enum": ["libscript-native", "system", "mise", "asdf"],
      "default": "libscript-native"
    }
  }
}
