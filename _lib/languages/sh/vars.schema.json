{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "/_lib/languages/sh/vars.schema.json",
  "title": "sh vars",
  "description": "Variables for Shell Toolchain",
  "type": "object",
  "properties": {
    "SH_INSTALL_METHOD": {
      "description": "How to install SH. 'system' uses the native OS package manager, 'libscript-native' downloads binaries natively.",
      "type": "string",
      "enum": ["system", "libscript-native", "mise", "asdf"],
      "default": "libscript-native"
    },
    "SH_VERSION": {
      "description": "Version of Shell Toolchain to install",
      "type": "string",
      "default": "0.5.12"
    }
  }
}
