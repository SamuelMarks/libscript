{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "/_lib/languages/sh/vars.schema.json",
  "title": "sh vars",
  "description": "Variables for Shell Toolchain",
  "type": "object",
  "properties": {
    "SH_INSTALL_METHOD": {
      "description": "How to install SH. 'system' uses the native OS package manager, 'source' compiles/downloads binaries.",
      "type": "string",
      "enum": [
        "system",
        "source"
      ],
      "default": "system"
    },
    "SH_VERSION": {
      "description": "Version of Shell Toolchain to install",
      "type": "string",
      "default": "latest"
    }
  }
}
