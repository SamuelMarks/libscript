{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "/_lib/languages/sh/vars.schema.json",
  "title": "sh vars",
  "description": "Variables for Shell Toolchain",
  "type": "object",
  "properties": {
    "SH_INSTALL_METHOD": {
      "description": "How to install SH. 'libscript_native' uses isolated version dirs, 'system' uses OS package manager, 'mise', 'asdf', 'pkgx', or 'vfox' defers to third-party tools.",
      "type": "string",
      "enum": ["libscript_native", "system", "mise", "asdf", "pkgx", "vfox"],
      "default": "libscript_native"
    },
    "SH_VERSION": {
      "description": "Version of Shell Toolchain to install",
      "type": "string",
      "default": "0.5.12"
    }
  }
}
