{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "/_lib/languages/nodejs/vars.schema.json",
  "title": "nodejs vars",
  "description": "Variables for Node.js Runtime",
  "type": "object",
  "properties": {
    "NODEJS_VERSION": {
      "description": "Version of Node.js demanded. Can be an alias or a specific numeric version number",
      "type": "string",
      "default": "lts",
      "version_aliases": ["lts", "latest"],
      "examples": ["lts", "latest", "v22.13.1", "20"]
    },
    "NODEJS_INSTALL_METHOD": {
      "description": "How to install NODEJS. 'libscript_native' uses isolated version dirs, 'system' uses OS package manager, 'mise', 'asdf', 'pkgx', or 'vfox' defers to third-party tools.",
      "type": "string",
      "enum": ["libscript_native", "system", "mise", "asdf", "pkgx", "vfox"],
      "default": "libscript_native"
    }
  }
}
