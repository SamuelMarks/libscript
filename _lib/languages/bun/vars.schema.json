{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "/_lib/languages/bun/vars.schema.json",
  "title": "bun vars",
  "description": "Variables for Bun Runtime",
  "type": "object",
  "properties": {
    "BUN_VERSION": {
      "description": "Version of Bun demanded. Can be a specific version number like 'bun-v1.1.0' or an alias.",
      "type": "string",
      "default": "latest",
      "versionAliases": [
        "latest",
        "canary"
      ]
    },
    "BUN_INSTALL_METHOD": {
      "description": "How to install BUN. 'system' uses the native OS package manager, 'source' compiles/downloads binaries.",
      "type": "string",
      "enum": [
        "system",
        "source"
      ],
      "default": "system"
    }
  }
}
