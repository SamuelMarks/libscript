{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "/_lib/languages/cpp/vars.schema.json",
  "title": "cpp vars",
  "description": "Variables for C++ Toolchain",
  "type": "object",
  "properties": {
    "CPP_INSTALL_METHOD": {
      "description": "How to install CPP. 'libscript_native' uses isolated version dirs, 'system' uses OS package manager, 'mise', 'asdf', 'pkgx', or 'vfox' defers to third-party tools.",
      "type": "string",
      "enum": ["libscript_native", "system", "mise", "asdf", "pkgx", "vfox"],
      "default": "system"
    },
    "CPP_VERSION": {
      "description": "Version of C++ Toolchain to install",
      "type": "string",
      "default": "latest"
    }
  }
}
