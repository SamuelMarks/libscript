{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "/_lib/languages/cpp/vars.schema.json",
  "title": "cpp vars",
  "description": "Variables for C++ Toolchain",
  "type": "object",
  "properties": {
    "CPP_INSTALL_METHOD": {
      "description": "How to install CPP. 'system' uses the native OS package manager, 'source' compiles/downloads binaries.",
      "type": "string",
      "enum": [
        "system",
        "source"
      ],
      "default": "system"
    },
    "CPP_VERSION": {
      "description": "Version of C++ Toolchain to install",
      "type": "string",
      "default": "latest"
    }
  }
}
