{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "/_lib/languages/go/vars.schema.json",
  "title": "go vars",
  "description": "Variables for Go Toolchain",
  "type": "object",
  "properties": {
    "GO_VERSION": {
      "description": "Specific version of go to install. Can be a numeric version or an alias.",
      "type": "string",
      "default": "latest",
      "versionAliases": [
        "latest",
        "stable"
      ]
    },
    "GO_INSTALL_METHOD": {
      "description": "How to install GO. 'system' uses the native OS package manager, 'source' compiles/downloads binaries.",
      "type": "string",
      "enum": [
        "system",
        "source"
      ],
      "default": "system"
    }
  }
}
