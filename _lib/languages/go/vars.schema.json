{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "/_lib/languages/go/vars.schema.json",
  "title": "go vars",
  "description": "Variables for Go Toolchain",
  "type": "object",
  "properties": {
    "GO_VERSION": {
      "description": "Specific version of go to install. Can be a numeric version or an alias.",
      "type": "string",
      "default": "latest",
      "version_aliases": ["latest", "stable"]
    },
    "GO_INSTALL_METHOD": {
      "description": "How to install GO. 'libscript-native' uses isolated version dirs, 'system' uses OS package manager, 'mise' or 'asdf' defers to third-party tools.",
      "type": "string",
      "enum": ["libscript-native", "system", "mise", "asdf"],
      "default": "libscript-native"
    }
  }
}
