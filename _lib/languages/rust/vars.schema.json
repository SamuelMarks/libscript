{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "/_lib/languages/rust/vars.schema.json",
  "title": "rust vars",
  "description": "Variables for Rust Toolchain",
  "type": "object",
  "properties": {
    "RUST_VERSION": {
      "description": "Version of Rust demanded. Can be \"nightly\"|\"beta\"|\"stable\" xor a specific version",
      "type": "string",
      "default": "stable",
      "version_aliases": ["stable", "beta", "nightly"]
    },
    "RUST_INSTALL_METHOD": {
      "description": "How to install RUST. 'libscript-native' uses isolated version dirs, 'rustup' uses rustup, 'system' uses OS package manager, 'mise' or 'asdf' defers to third-party tools.",
      "type": "string",
      "enum": ["libscript-native", "rustup", "system", "mise", "asdf"],
      "default": "libscript-native"
    }
  }
}
