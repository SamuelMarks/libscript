{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "/_lib/languages/rust/vars.schema.json",
  "title": "rust vars",
  "description": "Variables for Rust Toolchain",
  "type": "object",
  "properties": {
    "RUST_VERSION": {
      "description": "Version of Rust demanded. Can be \"nightly\"|\"beta\"|\"stable\" xor a specific version",
      "type": "string",
      "default": "stable",
      "versionAliases": [
        "stable",
        "beta",
        "nightly"
      ]
    },
    "RUST_INSTALL_METHOD": {
      "description": "How to install RUST. 'system' uses the native OS package manager, 'source' compiles/downloads binaries.",
      "type": "string",
      "enum": [
        "system",
        "source"
      ],
      "default": "system"
    }
  }
}
