{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "/_lib/languages/zig/vars.schema.json",
  "title": "zig vars",
  "description": "Variables for Zig Toolchain",
  "type": "object",
  "properties": {
    "ZIG_VERSION": {
      "description": "Specific version of zig to install. Can be a numeric version or an alias.",
      "type": "string",
      "default": "latest",
      "versionAliases": [
        "latest",
        "stable"
      ]
    },
    "ZIG_INSTALL_METHOD": {
      "description": "How to install ZIG. 'system' uses the native OS package manager, 'source' compiles/downloads binaries.",
      "type": "string",
      "enum": [
        "system",
        "source"
      ],
      "default": "system"
    }
  }
}
