{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "/_lib/languages/zig/vars.schema.json",
  "title": "zig vars",
  "description": "Variables for Zig Toolchain",
  "type": "object",
  "properties": {
    "ZIG_VERSION": {
      "description": "Specific version of zig to install. Can be a numeric version or an alias.",
      "type": "string",
      "default": "latest",
      "version_aliases": ["latest", "stable"]
    },
    "ZIG_INSTALL_METHOD": {
      "description": "How to install ZIG. 'libscript_native' uses isolated version dirs, 'system' uses OS package manager, 'mise', 'asdf', 'pkgx', or 'vfox' defers to third-party tools.",
      "type": "string",
      "enum": ["libscript_native", "system", "mise", "asdf", "pkgx", "vfox"],
      "default": "libscript_native"
    }
  }
}
