{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "/_lib/languages/kotlin/vars.schema.json",
  "title": "kotlin vars",
  "description": "Variables for Kotlin Toolchain",
  "type": "object",
  "properties": {
    "KOTLIN_INSTALL_METHOD": {
      "description": "How to install KOTLIN. 'libscript_native' uses isolated version dirs, 'system' uses OS package manager, 'mise', 'asdf', 'pkgx', or 'vfox' defers to third-party tools.",
      "type": "string",
      "enum": ["libscript_native", "system", "mise", "asdf", "pkgx", "vfox"],
      "default": "libscript_native"
    },
    "KOTLIN_VERSION": {
      "description": "Version of Kotlin Toolchain to install",
      "type": "string",
      "default": "1.9.20"
    }
  }
}
