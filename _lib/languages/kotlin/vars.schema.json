{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "/_lib/languages/kotlin/vars.schema.json",
  "title": "kotlin vars",
  "description": "Variables for Kotlin Toolchain",
  "type": "object",
  "properties": {
    "KOTLIN_INSTALL_METHOD": {
      "description": "How to install KOTLIN. 'system' uses the native OS package manager, 'libscript-native' downloads binaries natively.",
      "type": "string",
      "enum": ["system", "libscript-native", "mise", "asdf"],
      "default": "libscript-native"
    },
    "KOTLIN_VERSION": {
      "description": "Version of Kotlin Toolchain to install",
      "type": "string",
      "default": "1.9.20"
    }
  }
}
