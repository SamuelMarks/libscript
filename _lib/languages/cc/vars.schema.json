{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "/_lib/languages/cc/vars.schema.json",
  "title": "cc vars",
  "description": "Variables for C Compiler",
  "type": "object",
  "properties": {
    "CC_INSTALL_METHOD": {
      "description": "How to install CC. 'system' uses the native OS package manager, 'source' compiles/downloads binaries.",
      "type": "string",
      "enum": [
        "system",
        "source"
      ],
      "default": "system"
    },
    "CC_VERSION": {
      "description": "Version of C Compiler to install",
      "type": "string",
      "default": "latest"
    }
  }
}
