{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "/_lib/languages/cc/vars.schema.json",
  "title": "cc vars",
  "description": "Variables for C Compiler",
  "type": "object",
  "properties": {
    "CC_INSTALL_METHOD": {
      "description": "How to install CC. 'libscript_native' uses isolated version dirs, 'system' uses OS package manager, 'mise', 'asdf', 'pkgx', or 'vfox' defers to third-party tools.",
      "type": "string",
      "enum": ["libscript_native", "system", "mise", "asdf", "pkgx", "vfox"],
      "default": "system"
    },
    "CC_VERSION": {
      "description": "Version of C Compiler to install",
      "type": "string",
      "default": "latest"
    }
  }
}
