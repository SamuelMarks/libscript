{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "/_lib/languages/c/vars.schema.json",
  "title": "c vars",
  "description": "Variables for C Toolchain",
  "type": "object",
  "properties": {
    "C_INSTALL_METHOD": {
      "description": "How to install C. 'system' uses the native OS package manager, 'source' compiles/downloads binaries.",
      "type": "string",
      "enum": [
        "system",
        "source"
      ],
      "default": "system"
    },
    "C_VERSION": {
      "description": "Version of C Toolchain to install",
      "type": "string",
      "default": "latest"
    }
  }
}
