{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "/_lib/languages/csharp/vars.schema.json",
  "title": "csharp vars",
  "description": "Variables for C# Toolchain",
  "type": "object",
  "properties": {
    "CSHARP_INSTALL_METHOD": {
      "description": "How to install CSHARP. 'libscript-native' uses isolated version dirs, 'system' uses OS package manager, 'mise' or 'asdf' defers to third-party tools.",
      "type": "string",
      "enum": ["libscript-native", "system", "mise", "asdf"],
      "default": "libscript-native"
    },
    "CSHARP_VERSION": {
      "description": "Version of C# Toolchain to install",
      "type": "string",
      "default": "latest"
    }
  }
}
