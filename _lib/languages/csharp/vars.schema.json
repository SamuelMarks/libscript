{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "/_lib/languages/csharp/vars.schema.json",
  "title": "csharp vars",
  "description": "Variables for C# Toolchain",
  "type": "object",
  "properties": {
    "CSHARP_INSTALL_METHOD": {
      "description": "How to install CSHARP. 'system' uses the native OS package manager, 'source' compiles/downloads binaries.",
      "type": "string",
      "enum": [
        "system",
        "source"
      ],
      "default": "system"
    },
    "CSHARP_VERSION": {
      "description": "Version of C# Toolchain to install",
      "type": "string",
      "default": "latest"
    }
  }
}
