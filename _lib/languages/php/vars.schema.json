{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "/_lib/languages/php/vars.schema.json",
  "title": "php vars",
  "description": "Variables for PHP Runtime",
  "type": "object",
  "properties": {
    "PHP_INSTALL_METHOD": {
      "description": "How to install PHP. 'system' uses the native OS package manager, 'source' compiles/downloads binaries.",
      "type": "string",
      "enum": [
        "system",
        "source"
      ],
      "default": "system"
    },
    "PHP_VERSION": {
      "description": "Version of PHP Runtime to install",
      "type": "string",
      "default": "latest"
    }
  }
}
