{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "/_lib/languages/php/vars.schema.json",
  "title": "php vars",
  "description": "Variables for PHP Runtime",
  "type": "object",
  "properties": {
    "PHP_INSTALL_METHOD": {
      "description": "How to install PHP. 'libscript-native' uses isolated version dirs, 'system' uses OS package manager, 'mise' or 'asdf' defers to third-party tools.",
      "type": "string",
      "enum": ["libscript-native", "system", "mise", "asdf"],
      "default": "libscript-native"
    },
    "PHP_VERSION": {
      "description": "Version of PHP Runtime to install",
      "type": "string",
      "default": "latest"
    }
  }
}
