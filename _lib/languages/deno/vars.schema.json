{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "/_lib/languages/deno/vars.schema.json",
  "title": "deno vars",
  "description": "Variables for Deno Runtime",
  "type": "object",
  "properties": {
    "DENO_INSTALL_METHOD": {
      "description": "How to install DENO. 'libscript_native' uses isolated version dirs, 'system' uses OS package manager, 'mise', 'asdf', 'pkgx', or 'vfox' defers to third-party tools.",
      "type": "string",
      "enum": ["libscript_native", "system", "mise", "asdf", "pkgx", "vfox"],
      "default": "libscript_native"
    },
    "DENO_VERSION": {
      "description": "Version of Deno Runtime to install",
      "type": "string",
      "default": "latest"
    }
  }
}
