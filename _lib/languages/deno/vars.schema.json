{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "/_lib/languages/deno/vars.schema.json",
  "title": "deno vars",
  "description": "Variables for Deno Runtime",
  "type": "object",
  "properties": {
    "DENO_INSTALL_METHOD": {
      "description": "How to install DENO. 'system' uses the native OS package manager, 'source' compiles/downloads binaries.",
      "type": "string",
      "enum": [
        "system",
        "source"
      ],
      "default": "system"
    },
    "DENO_VERSION": {
      "description": "Version of Deno Runtime to install",
      "type": "string",
      "default": "latest"
    }
  }
}
