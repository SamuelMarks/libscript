{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "/_lib/languages/java/vars.schema.json",
  "title": "java vars",
  "description": "Variables for Java Toolchain",
  "type": "object",
  "properties": {
    "JAVA_INSTALL_METHOD": {
      "description": "How to install JAVA. 'system' uses the native OS package manager, 'source' compiles/downloads binaries.",
      "type": "string",
      "enum": [
        "system",
        "source"
      ],
      "default": "system"
    },
    "JAVA_VERSION": {
      "description": "Version of Java Toolchain to install",
      "type": "string",
      "default": "latest"
    }
  }
}
