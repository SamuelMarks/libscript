{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "/_lib/languages/java/vars.schema.json",
  "title": "java vars",
  "description": "Variables for Java Toolchain",
  "type": "object",
  "properties": {
    "JAVA_INSTALL_METHOD": {
      "description": "How to install JAVA. 'libscript_native' uses isolated version dirs, 'system' uses OS package manager, 'mise', 'asdf', 'pkgx', or 'vfox' defers to third-party tools.",
      "type": "string",
      "enum": ["libscript_native", "system", "mise", "asdf", "pkgx", "vfox"],
      "default": "libscript_native"
    },
    "JAVA_VERSION": {
      "description": "Version of Java Toolchain to install",
      "type": "string",
      "default": "latest"
    }
  }
}
