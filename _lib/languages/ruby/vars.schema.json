{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "/_lib/languages/ruby/vars.schema.json",
  "title": "ruby vars",
  "description": "Variables for Ruby Runtime",
  "type": "object",
  "properties": {
    "RUBY_VERSION": {
      "description": "Specific version of ruby to install. Can be a numeric version or an alias.",
      "type": "string",
      "default": "latest",
      "versionAliases": [
        "latest",
        "stable"
      ]
    },
    "RUBY_INSTALL_METHOD": {
      "description": "How to install RUBY. 'system' uses the native OS package manager, 'source' compiles/downloads binaries.",
      "type": "string",
      "enum": [
        "system",
        "source"
      ],
      "default": "system"
    }
  }
}
