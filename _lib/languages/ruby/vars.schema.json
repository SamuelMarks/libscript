{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "/_lib/languages/ruby/vars.schema.json",
  "title": "ruby vars",
  "description": "Variables for Ruby Runtime",
  "type": "object",
  "properties": {
    "RUBY_VERSION": {
      "description": "Specific version of ruby to install. Can be a numeric version or an alias.",
      "type": "string",
      "default": "latest",
      "version_aliases": ["latest", "stable"]
    },
    "RUBY_INSTALL_METHOD": {
      "description": "How to install RUBY. 'libscript_native' uses isolated version dirs, 'system' uses OS package manager, 'mise', 'asdf', 'pkgx', or 'vfox' defers to third-party tools.",
      "type": "string",
      "enum": ["libscript_native", "system", "mise", "asdf", "pkgx", "vfox"],
      "default": "libscript_native"
    }
  }
}
