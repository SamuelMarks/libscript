{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "/_lib/languages/swift/vars.schema.json",
  "title": "swift vars",
  "description": "Variables for Swift Toolchain",
  "type": "object",
  "properties": {
    "SWIFT_INSTALL_METHOD": {
      "description": "How to install SWIFT. 'system' uses the native OS package manager, 'source' compiles/downloads binaries.",
      "type": "string",
      "enum": [
        "system",
        "source"
      ],
      "default": "system"
    },
    "SWIFT_VERSION": {
      "description": "Version of Swift Toolchain to install",
      "type": "string",
      "default": "latest"
    }
  }
}
