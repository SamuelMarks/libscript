{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "/_lib/languages/swift/vars.schema.json",
  "title": "swift vars",
  "description": "Variables for Swift Toolchain",
  "type": "object",
  "properties": {
    "SWIFT_INSTALL_METHOD": {
      "description": "How to install SWIFT. 'libscript-native' uses isolated version dirs, 'system' uses OS package manager, 'mise' or 'asdf' defers to third-party tools.",
      "type": "string",
      "enum": ["libscript-native", "system", "mise", "asdf"],
      "default": "libscript-native"
    },
    "SWIFT_VERSION": {
      "description": "Version of Swift Toolchain to install",
      "type": "string",
      "default": "5.10"
    }
  }
}
