# LibScript REST API Scripts

## Overview

Build, test, and bootstrap automation for the LibScript REST API C server.

## Scripts

- `bootstrap.sh` / `.cmd` / `.ps1`: Resolves build prerequisites, sets up dependencies, and compiles
  the REST API server.
